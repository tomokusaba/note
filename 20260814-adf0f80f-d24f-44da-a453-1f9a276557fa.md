# 2026年8月14日 .NET の AI ルーティングと GitHub のモデル・ライセンス情報更新

AI を組み込んだ .NET アプリでは、要求ごとに利用するモデルやプロバイダーを選び、障害時に切り替えるための基盤が `Microsoft.Extensions.AI` に追加されました。GitHub では、Copilot の新しい選択可能モデルと、依存関係ライセンス情報の精度を高める更新が公開されています。

## .NET Blog

### [Microsoft.Extensions.AI でのルーティングとフェイルオーバー](https://devblogs.microsoft.com/dotnet/routing-and-failover-for-microsoft-extensions-ai/)

`Microsoft.Extensions.AI` に、モデルやプロバイダーの選択を `IChatClient` に組み込む実験的なプリミティブが追加されました。コスト、待ち時間、可用性を要求ごとに調整したい場面に使えます。

中心となるのは、選択処理を定義する `RoutingChatClient`、入力内容の埋め込み類似度で振り分ける `SemanticRoutingChatClient`、応答の送信前に失敗したときだけ別のクライアントを再選択する `FailoverChatClient`、優先順に切り替える `OrderedFailoverChatClient` です。

特にストリーミング応答では、利用者へ出力を返した後にプロバイダーを切り替えることはできません。記事では、複数ターンの会話で毎回ルーティングすると、プロバイダー固有の会話状態やプロンプトキャッシュを失う点にも触れています。会話単位で選択結果を保持する設計が必要です。

## GitHub Changelog

### [Gemini 3.7 Flash を GitHub Copilot で段階的に提供](https://github.blog/changelog/2026-08-13-gemini-3-7-flash-is-now-available-in-github-copilot)

GitHub Copilot のモデル選択に Gemini 3.7 Flash が段階的に加わります。Pro、Pro+、Max、Business、Enterprise の利用者が対象で、VS Code、Visual Studio、Copilot CLI、GitHub Copilot アプリ、各種 IDE などで選択できます。

Business と Enterprise では、管理者が Copilot 設定で **Gemini 3.7 Flash Preview** ポリシーを有効にする必要があります。利用量課金ではプロバイダーのリスト価格で請求されるため、導入前に料金と組織の利用ポリシーを確認しておくとよさそうです。

### [依存関係ライセンス情報の品質を改善](https://github.blog/changelog/2026-08-13-license-data-quality-improvements)

GitHub の依存関係グラフは、ライセンス情報の主な取得元を ClearlyDefined から各パッケージエコシステムの公式レジストリへ変更しました。npm は npmjs.org、NuGet は nuget.org、Python は PyPI といった正規のメタデータを優先し、既存の ClearlyDefined は補完として利用されます。

この変更は Dependency insights、SBOM、GitHub Advanced Security のオープンソースライセンスコンプライアンス、Dependency Review Action に反映されます。GitHub の初期計測では、依存関係グラフ内の 1 億 7,000 万パッケージにおけるライセンス未判定の割合が 45% から 24% へ下がりました。

さらに、ライセンス履歴を個別バージョンではなくバージョン範囲で管理するため、新しいパッケージバージョンでも情報を利用しやすくなります。依存関係のライセンス確認をリリース判定へ組み込んでいるチームは、表示結果の変化を確認しておく価値があります。

---

## まとめ

- `Microsoft.Extensions.AI` で、内容に応じたモデル選択と障害時のフェイルオーバーを `IChatClient` として実装できる
- GitHub Copilot に Gemini 3.7 Flash が追加され、Business と Enterprise では管理者によるポリシー有効化が必要になる
- GitHub の依存関係ライセンス情報は公式レジストリを優先する仕組みに変わり、SBOM やレビューで参照する情報の網羅性が向上する
