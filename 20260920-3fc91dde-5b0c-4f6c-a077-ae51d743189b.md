# 2026-09-20 OSS PR サマリー — 主要リポジトリ動向

## はじめに

- 確認日時: 2026-09-20 06:02:05 JST（UTC 2026-09-19 21:02:05）
- 集計範囲: `merged:>=2026-09-19` を UTC 基準で確認し、`mergedAt` を用いて対象期間に含まれる PR だけを採用
- 対象リポジトリ: microsoft/fluentui-blazor, microsoft/fluentui, microsoft/aspire, dotnet/runtime, microsoft/agent-framework, m5stack/StackChan

今回の 1 日集計では、Azure Hosting の安定性改善と .NET runtime の JIT / 暗号基盤の修正が目立ちました。一方で、Fluent UI 系、Agent Framework、StackChan は対象期間内にマージ済みの PR を確認できませんでした。

## microsoft/fluentui-blazor

集計期間内にマージされた PR はありませんでした。

## microsoft/fluentui

集計期間内にマージされた PR はありませんでした。

## microsoft/aspire

1. **[#20249] Improve Azure hosting test logging and stability**
   - Azure Hosting の構成テストで、不要にコンテナやアプリを起動していた問題を解消した。
   - RemoteExecutor のログを親テストに適切に転送させるように改善し、CI での失敗原因の特定がしやすくなった。
   - 変更の中心は Azure Hosting のテスト安定性とログ観測性で、パイプラインの信頼性向上に直結する。
   - [PR を見る](https://github.com/microsoft/aspire/pull/20249?WT.mc_id=DT-MVP-5004827)

## dotnet/runtime

### JIT / 最適化

1. **[#134205] JIT: Validate jump threading phi inputs**
   - JIT の jump threading で、phi の入力が不完全な状態で書き換えられる不具合を修正した。
   - 前提条件の検証が不十分な場合は安全にスキップするように変更され、既存アプリの不定動作を抑える意図が明確だ。
   - 回帰テストとスーパー PMI の検証も行われており、最適化の安全性が高まっている。
   - [PR を見る](https://github.com/dotnet/runtime/pull/134205?WT.mc_id=DT-MVP-5004827)

3. **[#134198] JIT: Make LACKS_RETURN callsite-specific**
   - inliner の `LACKS_RETURN` 判定を callsite ごとに扱うように変更し、1 回の失敗で関数全体が固定的に non-inline 扱いになるケースを減らした。
   - JIT 最適化の判定がより現実的になり、性能に影響する最適化の再現性が向上している。
   - [PR を見る](https://github.com/dotnet/runtime/pull/134198?WT.mc_id=DT-MVP-5004827)

5. **[#134224] Remove open delegate Equals fastpath**
   - open delegate の `Equals` の高速化パスを削除し、仮想メソッド時の不整合を避ける安全策を入れた。
   - 変更自体は小さいが、JIT とメソッド呼び出しの境界で起き得る不正な経路を防ぐ位置づけの修正だ。
   - [PR を見る](https://github.com/dotnet/runtime/pull/134224?WT.mc_id=DT-MVP-5004827)

### 暗号 / PKI

1. **[#134162] [release/11.0] Expose Composite ML-DSA CNG identifiers**
   - .NET 11 RC2 向けに experimental な Composite ML-DSA CNG 識別子を公開した。
   - 暗号 API 利用者が、プロバイダー依存の文字列ではなく意味のある名前付き識別子を使えるようになり、利用者体験と互換性が向上する。
   - [PR を見る](https://github.com/dotnet/runtime/pull/134162?WT.mc_id=DT-MVP-5004827)

3. **[#134163] [release/11.0] Add Composite ML-KEM base APIs**
   - Composite ML-KEM の experimental API を追加し、将来の Post-Quantum Cryptography 対応の土台を整えた。
   - 既存動作を壊すものではないが、API surface の拡張としては大きく、ハイブリッド暗号の利用が広がる可能性がある。
   - [PR を見る](https://github.com/dotnet/runtime/pull/134163?WT.mc_id=DT-MVP-5004827)

5. **[#133763] Don't use IgnoreRootRevocationUnknown when processing intermediate CA re-key certificates**
   - 中間 CA の再鍵証明書処理時に `IgnoreRootRevocationUnknown` を使わないように変更し、証明書検証の誤判定を避けた。
   - 証明書チェーンの取り扱いがより安全になり、PKI の信頼性向上につながる大事な修正だ。
   - [PR を見る](https://github.com/dotnet/runtime/pull/133763?WT.mc_id=DT-MVP-5004827)

## microsoft/agent-framework

集計期間内にマージされた PR はありませんでした。

## m5stack/StackChan

集計期間内にマージされた PR はありませんでした。

## まとめ

- **microsoft/fluentui-blazor**: 集計期間内にマージされた PR はありませんでした。大きな変化は見られませんでした。
- **microsoft/fluentui**: 集計期間内にマージされた PR はありませんでした。Fluent UI 本体の動きは静かでした。
- **microsoft/aspire**: Azure Hosting のテスト安定性とログ観測性改善が中心で、CI の信頼性向上に効く修正が入っていました。
- **dotnet/runtime**: JIT の安全性修正と PQC / PKI 周辺の追加・検証改善が目立ち、runtime の基盤を強くする内容が多く見られました。
- **microsoft/agent-framework**: 集計期間内にマージされた PR はありませんでした。今回の対象期間では静かな状態でした。
- **m5stack/StackChan**: 集計期間内にマージされた PR はありませんでした。更新は確認できませんでした。

全体として、今回の 1 日の動向は「安定性の強化」と「次世代暗号基盤の整備」に集約されていました。特に .NET runtime は JIT の安全性修正と暗号・証明書周辺の改善が並び、基盤の健全性と将来の対応力を同時に高める方向性が強く出ています。一方で、Fluent UI 系や StackChan は対象期間に大きなマージイベントがなく、比較的静かな一日でした。
