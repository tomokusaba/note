# 2026-08-18 OSS PR サマリー — Microsoft/.NET と M5Stack の主要リポジトリ動向

## はじめに

本記事は、microsoft/fluentui-blazor、microsoft/fluentui、microsoft/aspire、dotnet/runtime、microsoft/agent-framework、m5stack/StackChan の 6 リポジトリを対象に、マージ済み Pull Request の動向をまとめたものです。確認日時は JST 2026-08-18T06:02:05.8543308+09:00、集計対象期間は UTC 2026-08-17T00:00:00Z から 2026-08-17T21:02:05.8585270Z までです。各 PR の変更内容に加え、PR 本文で明示されている範囲の破壊的変更の有無も確認します。

---

## microsoft/fluentui-blazor

集計期間内に 6 件の PR がマージされました。バグ修正、Native AOT / ビルドターゲット対応、機能追加・パッケージ更新に分けて紹介します。

### バグ修正

1. **[#5157] Fix overflow behavior deserialization**
   `FluentOverflow.ts` が返す `behavior` プロパティは `"fixed"` や `"ellipsis"` などの文字列ですが、対応する `OverflowItem.Behavior` は数値ベースの列挙型変換を前提としていたため、ブラウザからのペイロードでデシリアライズに失敗する場合がありました。AOT 対応の `JsonStringEnumConverter<OverflowBehavior>` を適用し、`"fixed"`・`"ellipsis"`・`null` を含む回帰テストを追加して修正しています。
   [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5157)

3. **[#5154] fix: [DataGrid] arrow key navigation scrolls the grid again, clears sticky headers and stops throwing at dataset edges**
   以前の変更で Drawer/Dialog 内のグリッドが親要素までスクロールしてしまう問題を `preventScroll: true` で防いだ結果、rc.5 以降は矢印キー操作でフォーカスがビューポート外に出てもグリッド自体がスクロールしなくなり、`Virtualize` 使用時は行の追加読み込みが止まって `matches` の TypeError が発生していました。本 PR は `scrollIntoView` の明示呼び出し、固定ヘッダーを避けるための `scroll-margin-block-start` 設定、範囲外セル参照時のガード追加という 3 つの修正でこの回帰を解消しています。
   [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5154)

### Native AOT / ビルドターゲット対応

1. **[#5156] Add Native AOT compatibility**
   Core コンポーネントおよび Icons/Emoji パッケージ群から、トリミングやシングルファイル、AOT アナライザーの警告を解消し、Native AOT 対応を宣言できるようにした変更です。`PropertyColumn` や `FluentField` のリフレクション処理を置き換え、テーマのシリアライズとデシリアライズにはソース生成ベースの `System.Text.Json` メタデータを利用しています。各アセンブリに `IsAotCompatible` を設定し、.NET 8 の Native AOT 実行ファイルでの動作確認も行っています。
   [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5156)

3. **[#5155] Target Fluent UI Icons package to .NET 8**
   Fluent UI Icons パッケージのターゲットフレームワークがハードコードされた `net9.0` になっていたのを、コアコンポーネントや Emoji パッケージと同じ共有設定 `AssetsNetVersion`（`net8.0`）に揃えた変更です。これにより .NET 8 の Blazor アプリからもパッケージ一式を参照できるようになりました。
   [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5155)

### 機能追加・パッケージ更新

1. **[#5145] [dev-v5] Add ControlStyle parameter to FluentSelect**
   `FluentSelect` に内部コントロールへスタイルを適用できる `ControlStyle` パラメーターを追加し、カスタマイズの幅を広げた変更です。あわせて CSS セレクターを `.control` から `:host .control` に修正し、対応するユニットテストも追加されています。
   [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5145)

3. **[#5139] [v4] Update package versions**
   ソリューション内の NuGet / npm パッケージのバージョンを更新するスクリプトを導入し、破壊的変更を伴わない形で依存関係を最新化しました。あわせて `AngleSharp.CSS` の更新が bUnit の前提とする `AngleSharp` のバージョンをずらし、テストが失敗する問題も見つかりました。この問題は `Directory.Packages.props` で `AngleSharp.CSS` のバージョンを明示的に固定して回避しています。
   [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5139)

---

## microsoft/fluentui

集計期間内にマージされた PR はありませんでした。

---

## microsoft/aspire

集計期間内に 7 件の PR がマージされました。バグ修正、インフラ / CI・テスト基盤、依存関係・機能追加に分けて紹介します。

### バグ修正

1. **[#19426] Fix mixed-quality staging package discovery**
   ステージング配布では大半のパッケージが安定版ですが、Azure Kubernetes のような一部の連携パッケージは意図的にプレリリースのまま提供される構成になっており、TypeScript/ポリグロット版の `aspire add` はステージングチャンネルを安定版限定として扱っていたため、それらのパッケージが検出されなくなっていました。本 PR はステージング identity のチャンネル構築に限定して安定版とプレリリースの両方を検出できるよう修正し、C# 側と TypeScript 側の検証済みマトリクスと回帰テストを追加しています。
   [PR を見る](https://github.com/microsoft/aspire/pull/19426)

3. **[#19400] Honor selected AppHost launch profile in VS Code**
   VS Code 拡張で `launch.json` から非デフォルトの AppHost 起動プロファイルを選択しても、古い Aspire CLI が既定プロファイルの環境変数をフラット化して送ってくる場合には、実際にはデフォルトプロファイルの環境変数と URL で起動してしまう不具合がありました。拡張側で既定プロファイル由来の値のみを取り除いたうえで選択されたプロファイルを .NET SDK の優先順位で適用するよう修正し、拡張側の更新のみで対応できる形にしています。
   [PR を見る](https://github.com/microsoft/aspire/pull/19400)

5. **[#19343] Tolerate NuGet credential-provider stdout preamble in package search parsing**
   認証済みの Azure DevOps / Azure Artifacts フィードを構成していると、NuGet Azure Artifacts Credential Provider が `dotnet package search --format json` の標準出力に JSON より前へ `[CredentialProvider]…` という進捗行を書き込みます。そのため、標準出力全体を 1 つの JSON として解析する既存処理が `JsonReaderException` で失敗していました。バックグラウンドのバージョンチェックが壊れ、`aspire add` が「パッケージが見つからない」と報告する影響を防ぐため、最初の `{` から JSON を切り出す共通ヘルパーを追加してすべての解析箇所に適用しています。
   [PR を見る](https://github.com/microsoft/aspire/pull/19343)

### インフラ / CI・テスト基盤

1. **[#19438] Upgrade gh-aw compiler to v0.86.2**
   リポジトリの 8 つのエージェント型ワークフローを安定版の gh-aw v0.86.2 へアップグレードし、生成されるロックファイルと `actions-lock.json` を更新した変更です。新しいコンパイラでは `tools.github.min-integrity` が `none` の場合にシェル許可リストの明示が必要になったため、対象ワークフローに読み取り専用のコマンド集合を追加しています。
   [PR を見る](https://github.com/microsoft/aspire/pull/19438)

3. **[#19402] Bound Dotnet project pipeline tests**
   `DotnetProjectResource` のパイプライン回帰テストが `CancellationToken.None` で実行されていたため、デッドロックや長時間化するリグレッションが発生するとテストランナーのセッションタイムアウトまで止まってしまう恐れがありました。各パイプライン実行に 30 秒のリンクされたキャンセルトークンを渡し、`PipelineContext` を通して待機処理に反映させることで、テストフレームワーク側のキャンセルも尊重しつつ実行時間の上限を設けています。
   [PR を見る](https://github.com/microsoft/aspire/pull/19402)

### 依存関係・機能追加

1. **[#19440] Update available external dependencies**
   社内フィードから既に取得可能な最新推奨バージョンの外部 NuGet 依存関係を更新した変更です。あわせて Qdrant.Client 1.19.0 で要求される `SearchAsync` から `QueryAsync` への移行をサンプル・テストの呼び出し箇所 3 か所で行い、OpenTelemetry 系パッケージも安定版が出ている 1.17.0 系に揃えています。
   [PR を見る](https://github.com/microsoft/aspire/pull/19440)

3. **[#19041] [Azure App Configuration] - Add health check endpoint for app config emulator**
   `RunAsEmulator` で動かす Azure App Configuration エミュレーターに、`/health` エンドポイントを使った HTTP ヘルスチェックを追加し、あわせてイメージを 1.2.0 に更新した変更です。これにより `OnResourceReady` はエミュレーターがリクエストを受け付けられる状態になってから実行されるようになります。
   [PR を見る](https://github.com/microsoft/aspire/pull/19041)

---

## dotnet/runtime

集計期間内に 12 件の PR がマージされました。件数が多いため、JIT / WebAssembly 基盤、API 改善・パフォーマンス、バグ修正、インフラ / CI・リリースの 4 カテゴリに分けて紹介します。

### JIT / WebAssembly 基盤

1. **[#132259] JIT: fill two gaps in wasm codegen**
   `genReturnSuspend` はエピローグの戻り値を型付けたままにするためネイティブ戻り値型のゼロ値を push していましたが、バッファ経由で返すメソッドは戻り値なしの wasm 関数へ落ちるため、その `compRetNativeType`（`TYP_STRUCT`）がデフォルト分岐に落ちて push するものがない状態になっていました。値渡しの `Vector128` 戻り値（`v128`）の扱いも欠けていたため、`System.Private.Xml` の crossgen 時に見つかったこれらのギャップを埋め、無引数の managed throw ヘルパーのシグネチャも不足分を追加しています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132259)

3. **[#131771] Add typed self-counting WASM sections**
   WASM オブジェクトライター刷新の一連の PR の 2 本目で、`WasmSection` と `WebCilSection` に共通する `SectionWriter` のデータ出力処理をまとめ、インポートや関数、グローバル、エクスポート、要素向けに件数を自動追跡する型付きベクターセクションを追加しました。手動のカウンターやカウント値の書き換え処理を削除しつつ、出力されるモジュール構造と既存の挙動は変えていません。
   [PR を見る](https://github.com/dotnet/runtime/pull/131771)

### API 改善・パフォーマンス

1. **[#132411] [System.Text.Json] Add structural union type classifier**
   承認済みの `JsonUnionTypeStructuralClassifier` API を追加し、JSON のトークン種別と同じ階層のオブジェクトプロパティ名によってユニオン型の各ケースを分類できるようにした変更です。あいまい・非対応な構成はクラシファイア構築時に拒否するようにし、64 個を超えるオブジェクトケースを含むユニオンなど、これまでのテスト専用プロトタイプをリゾルバー非依存のエンドツーエンドテストに置き換えています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132411)

3. **[#132367] Make the Nullable<T>.Value exception message actionable**
   値を持たない `Nullable<T>` に `.Value` でアクセスした際の例外メッセージ「Nullable object must have a value.」は、実際に取るべき行動（`.HasValue` を先に確認すること）を示していませんでした。メッセージを「Cannot read the Value property of a Nullable object that has no value. Check HasValue before reading Value.」に変更し、リソース名自体は変えずに `System.Private.CoreLib` と NativeAOT 側の両方のリソースファイルを更新しています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132367)

5. **[#132398] Use GC.AllocateUninitializedArray in File.ReadAllBytes[Async]**
   `File.ReadAllBytes` / `ReadAllBytesAsync` は結果バッファを `new byte[count]` で確保しており、読み取りループがすぐに上書きするゼロクリアのコストを払っていました。バッファ確保を `GC.AllocateUninitializedArray<byte>(count)` に置き換え、読み取り不足時は例外を投げて未初期化メモリが外部に漏れないようにしたうえで、ベンチマークでは 10MB の読み取りで同期約 14%、非同期約 24% 高速化したと報告されています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132398)

### バグ修正

1. **[#132274] Fix DeflateEncoder.GetMaxCompressedLength conservative bound**
   従来の実装は zlib の `compressBound()` を利用していましたが、これは既定設定での `compress()` API 向けの保守的な上限です。そのため、.NET 側が使うカスタムパラメータ付きの `deflate` 直接呼び出しでは正しい上限になりませんでした。パラメータ依存の `deflateBound` を静的 API から呼び出すのは難しいため、.NET API が公開する圧縮設定ごとの最大上限を zlib-ng の `deflate.c` の計算式から求める形で回避策を実装し、maccatalyst・iOS シミュレーター相当のロジックで検証しています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132274)

3. **[#129853] fix shutdown tests**
   Helix 環境では dotnet が PATH 上になかったり、プロセス起動が一時的に失敗したりすることがあり、機能テストのデプロイヤーがそれに弱い状態でした。既定の dotnet ホストをグローバル PATH ではなくテストホストのレイアウトから解決するように変更し、Process オブジェクトが起動に至らなかった場合でも後片付けで例外を投げないようにしたうえで、自己ホストプロセス起動に 3 回までのリトライを追加しています。
   [PR を見る](https://github.com/dotnet/runtime/pull/129853)

### インフラ / CI・リリース

1. **[#132405] Add merge automation for 11.0rc1/rc2 release branches**
   タイトルの通り、.NET 11.0 の rc1 / rc2 リリースブランチ向けにマージ自動化を追加するインフラ整備で、PR 本文に詳細な説明は記載されていません。
   [PR を見る](https://github.com/dotnet/runtime/pull/132405)

3. **[#132402] Use stable net/netframework folders for the mono SDK tasks**
   mono / WebAssembly SDK 系のタスクアセンブリは `tasks/net11.0` のように TFM 別のサブフォルダーに配置されており、.NET Core の TFM が上がるたびにレイアウトと消費側の双方を連動して更新する必要がありました。以前 `Microsoft.NET.Sdk.WebAssembly.Pack.Tasks` に行ったのと同様に、出力先を TFM に依存しない `net` / `netframework` フォルダーへ切り替え、`WasmAppBuilder` や `MonoAOTCompiler` など 10 個のタスクプロジェクトと関連する `.in` テンプレートを整理しています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132402)

5. **[#132376] Upgrade gh-aw compiler to v0.86.2**
   CI の Outer-Loop Failure Scanner は、Copilot CLI のツールキャッシュヒット時に `PATH` へキャッシュディレクトリが追加されるものの、生成されたハーネスが絶対パス `/usr/local/bin/copilot` を呼び出すため `ENOENT` で失敗する状態になっていました。gh-aw コンパイラーと setup アクションを v0.85.4 から v0.86.2 へ上げてこの修正を取り込み、6 つのエージェント型ワークフローのロックファイルを再生成しています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132376)

7. **[#132047] Enable Azure Pipelines reporter in paltests.proj**
   PALTests レグでビルド解析（build analysis）レポーターを有効化する変更で、関連する issue に紐づく CI 整備として行われています。
   [PR を見る](https://github.com/dotnet/runtime/pull/132047)

9. **[#132363] [wasm] Bump chrome for testing - linux: 151.0.7922.137, windows: 152.0.7977.42, mac: 152.0.7977.42**
   自動化ボット（app/github-actions）による、WASM テストで使用する Chrome for Testing の Linux・Windows・macOS 向けバージョンバンプです。
   [PR を見る](https://github.com/dotnet/runtime/pull/132363)

---

## microsoft/agent-framework

集計期間内に 7 件の PR がマージされました。エージェント実行・セッション機能拡張、ワークフロー・ツール連携、ドキュメント・サンプル修正に分けて紹介します。

### エージェント実行・セッション機能拡張

1. **[#7650] .NET: Allow agents to opt into concurrent tool invocation**
   `AsAIAgent()` が生成する既定の `FunctionInvokingChatClient` は、モデルが複数の独立したツール呼び出しを返した場合でも常に直列実行になっていました。後方互換を保ったまま並行実行を選べるよう `ChatClientAgentOptions.AllowConcurrentInvocation` を追加し、既定の関数呼び出しミドルウェアやオプションのクローン処理にも反映してユニットテストを追加しています。
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7650)

3. **[#7648] .NET: Add Foundry hosted session and user identity pass-through**
   Foundry のホスト型エージェント呼び出しに、同じコンテナサンドボックスを再利用するための `agent_session_id`（セッション間で維持）と、1 つの中間層アプリが複数のエンドユーザーを区別して呼び出せる `x-ms-user-identity` ヘッダーという 2 つの独立した制御を追加しています。`ChatOptions` の拡張メソッドやセッション作成 API、リクエストパイプラインの対応する処理を実装し、異なるユーザー識別子ごとに別々の `AgentSession` を使う必要がある点をユニットテストと、既定では実行されない結合テストで確認しています。
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7648)

5. **[#7412] .NET: Add Cosmos chat history retrieval API**
   `CosmosChatHistoryProvider` はメッセージの件数取得や削除の機能は持ちますが、エージェントの呼び出しパイプラインを実行せずに保存済みメッセージそのものを取得する手段がなく、移行や調査といった用途で不便でした。セッションを指定してメッセージを取得する `GetMessagesAsync` を追加し、既存の呼び出しパイプラインと同じ Cosmos クエリ・ページング処理を共有させたうえで、フィードイテレーターの破棄処理やページング・空履歴などのテストも追加しています。
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7412)

### ワークフロー・ツール連携

1. **[#7636] Python: Add a global workflow checkpoint type registry**
   チェックポイントストレージへ直接アクセスできないコード（Foundry Hosted Agent など）からでも型を登録できるよう、グローバルなチェックポイント型レジストリと登録用ヘルパー `register_checkpoint_type` を追加した変更です。
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7636)

3. **[#7606] Python: Surface A2A preview consent URLs**
   Work IQ のツールソースが `type: "a2a_preview"` を伴う `CONSENT_REQUIRED` を返した場合、Foundry Hosting のレスポンスサーバーは `mcp` ソースしか受け付けていなかったため、同意 URL が呼び出し元に渡されていませんでした。`consent_url_from_error` が `mcp` と `a2a_preview` の両方の同意ソースを認識できるよう修正し、回帰テストを追加しています。
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7606)

### ドキュメント・サンプル修正

1. **[#7693] Python: Fix Agent Lightning installation link**
   Agent Framework Lab Lightning の README がリンクしていた Agent Lightning のインストールページが HTTP 404 になっており、対応バージョンである 0.2.x〜0.3.x 向けの説明としては、リデザインされた 1.0 系の最新ドキュメントではなくバージョン指定された 0.3.0 のドキュメントへリンクし直す必要がありました。本 PR はリンク先を `agentlightning>=0.2.0,<0.4.0` に対応する 0.3.0 のインストールガイドへ差し替えています。
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7693)

3. **[#7674] .NET: Fix declarative workflows deep research sample**
   `DeepResearch` の宣言的ワークフローサンプルが壊れており、有効な否定的な結果を「未完了」として誤判定してしまう問題がありました。チーム入力を構造化し、完了条件を明確化したうえでサンプルプロンプトを追加し、プランナーの再利用性とマネージャーの判断精度を改善しています。
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7674)

---

## m5stack/StackChan

集計期間内にマージされた PR はありませんでした。

---

## まとめ

- **microsoft/fluentui-blazor**（6 件）: `FluentOverflow` の JSON デシリアライズや DataGrid の矢印キー操作のバグ修正、Native AOT 対応、FluentSelect への `ControlStyle` 追加など
- **microsoft/fluentui**（0 件）: 集計期間内にマージされた PR はありませんでした
- **microsoft/aspire**（7 件）: CLI のステージングパッケージ検出や VS Code の起動プロファイル、NuGet 資格情報プロバイダー出力の解析といったバグ修正、gh-aw アップグレードやパイプラインテストの時間上限設定などの CI 強化、Azure App Configuration エミュレーターへのヘルスチェック追加
- **dotnet/runtime**（12 件）: WASM コード生成や `System.Text.Json` のユニオン型分類器などの JIT / API 改善、`File.ReadAllBytes` の高速化、Deflate 圧縮上限やシャットダウンテストの修正、リリースブランチ・CI 基盤の整備
- **microsoft/agent-framework**（7 件）: .NET 向けの並行ツール呼び出しオプトインや Foundry ホストセッション、Cosmos チャット履歴取得 API などのエージェント機能拡張、Python 向けのワークフローチェックポイントレジストリや A2A 同意 URL 対応
- **m5stack/StackChan**（0 件）: 集計期間内にマージされた PR はありませんでした

集計対象とした 32 件の PR のうち、本文やチェックリストで破壊的変更（breaking change）と明記されたものはありませんでした。microsoft/agent-framework の各 PR は「This is not a breaking change」のチェック項目で非破壊的変更であることを明示しており、他リポジトリの PR 本文にも破壊的変更を示す記述は見当たりませんでした。