# 2026-08-06 OSS PR サマリー — Microsoft / .NET / M5Stack 主要リポジトリ動向

2026-08-06 06:02 JST 時点で、Microsoft / .NET / M5Stack に関わる 6 リポジトリのマージ済み PR を確認しました。対象は **Fluent UI Blazor**、**Fluent UI**、**.NET Aspire**、**.NET Runtime**、**Microsoft Agent Framework**、**StackChan** です。

集計対象の UTC 範囲は `2026-08-04 00:00:00 UTC` から `2026-08-05 21:02:25 UTC` までです。UI、開発基盤、実行基盤、エージェントまでを横断して、今回マージされた 91 件を簡潔に振り返ります。

本記事は、対象リポジトリを追っている開発者向けの全件一覧です。互換性への影響を先に確認したい場合は、本文中の **破壊的変更** から確認してください。

## Fluent UI Blazor

Fluent UI Blazor は、Microsoft Fluent UI を Blazor 向けコンポーネントとして提供するリポジトリです。今回は v5 系のメニュー、DataGrid、アクセシビリティに関する更新が中心でした。

### コンポーネントとアクセシビリティ

1. **#5080 — FluentMenu に遅延レンダリング用の `RenderWhen` を追加**  
`RenderWhen` パラメーターで、必要になるまでメニューを描画しないようにする変更です。条件が `true` になるとメニューを初期化して開き、未指定時は従来どおり常に描画されます。[PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5080)

3. **#5079 — `beforeStart` と `afterStarted` を再エクスポート**  
BlazorWebView（WPF、MAUI、WinForms）で必要な従来のフックを再エクスポートする PR です。これらの環境では新しい形式のフックをサポートしないことが理由として示されています。[PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5079)

5. **#5075 — DataGrid の CSS クラス利用を属性へリファクタリング** **破壊的変更**  
DataGrid のスタイル指定を CSS クラスから属性中心へ置き換えるリファクタリングです。既存のクラス名に依存する実装は、属性に合わせた変更が必要です。[PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5075)

7. **#5074 — FluentSelect のアクセシビリティを改善**  
FluentSelect の初期化処理と JavaScript 相互運用を見直す変更です。コンボボックスの値設定と、`aria-label`・`aria-expanded` の追加が含まれます。[PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5074)

9. **#5073 — ボタンコンポーネントへ `role` 属性を追加**  
ボタンコンポーネントに適切な `role` 属性を加えるアクセシビリティ改善です。スクリーンリーダーなどの支援技術がボタンを解釈しやすくすることを目的にしています。[PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5073)

---

## Fluent UI

Fluent UI は、React、Web Components などを含む Microsoft のデザインシステム実装を管理するリポジトリです。今回はヘッドレス API のバンドル削減と、ESM-first 移行を支える開発基盤の変更が目立ちます。

### コンポーネント、アイコン、バンドル

1. **#36507 — `clpx` 拡張子を Clipchamp アイコンへ対応付け**  
`FileTypeIconMap` に `clpx` 拡張子を追加し、既存の Clipchamp アイコンを利用する変更です。新しいアイコン資産や CDN URL の追加は不要と説明されています。[PR を見る](https://github.com/microsoft/fluentui/pull/36507)

3. **#36506 — 失敗したリリース後の Web Components バージョンを同期**  
公開済みの Web Components パッケージのバージョンと、リポジトリ内のマニフェスト・変更履歴・ロックファイルを同期する PR です。これは既存の npm リリースを記録するためのもので、新たなバージョンアップは行いません。[PR を見る](https://github.com/microsoft/fluentui/pull/36506)

5. **#36505 — `react-tabster` を Tabster 9 canary へ更新**  
`react-tabster` の依存関係を `tabster@9.0.0-canary.0` へ更新する変更です。スタンドアロンの focus finder API への対応と、互換性維持用の export が含まれます。[PR を見る](https://github.com/microsoft/fluentui/pull/36505)

7. **#36504 — TeachingPopover のヘッドレス入口から dismiss アイコンを分離**  
`TeachingPopoverTitle` の状態用フックを、アイコンを含まないモジュールへ移す修正です。ヘッドレスな TeachingPopover の入口が dismiss アイコンと Griffel を取り込まないようにします。[PR を見る](https://github.com/microsoft/fluentui/pull/36504)

9. **#36503 — TagPicker のヘッドレス状態から既定アイコンを除去**  
`TagPickerControl` の状態と既定の chevron アイコンを分離する変更です。利用者が slot を渡した場合だけ、ヘッドレス側の `expandIcon` を描画します。[PR を見る](https://github.com/microsoft/fluentui/pull/36503)

### Web Components と ESM-first の基盤

1. **#36497 — Combobox と TagPicker の共通フックから Tabster 処理を分離**  
Escape-ignore 用の Tabster 属性を必要なラッパー処理だけへ移す修正です。ポップアップ表示時の Escape 動作を保ちながら、ヘッドレスバンドルの余分な負荷を削減します。[PR を見る](https://github.com/microsoft/fluentui/pull/36497)

3. **#36457 — Web Components の `data-indent` 判定を CSS ベースへ移行**  
Menu List、Tree Item、Tablist で JavaScript により `data-indent` を付与するパターンをなくす変更です。`:scope:has()` と subgrid を使って、スロットの有無と親のグリッドに基づくスタイルへ切り替えます。[PR を見る](https://github.com/microsoft/fluentui/pull/36457)

5. **#36362 — `migrate-converged-pkg` の ESM-first 出力を `type: module` で opt-in 化**  
移行ジェネレーターが ESM-first の形を出力する条件を、各パッケージの `type: module` に限定する変更です。CommonJS パッケージの既存の出力を変えずに、ESM を選んだパッケージだけを対象にします。[PR を見る](https://github.com/microsoft/fluentui/pull/36362)

7. **#36360 — `.cjs` 設定の認識と任意の `attw` ターゲットを追加**  
ビルド・テスト対象の推論で `.cjs` 設定ファイルを認識し、ESM パッケージを扱えるようにする変更です。`exports` を持つ公開可能な v9 ライブラリ向けに、任意の `attw` ターゲットも追加します。[PR を見る](https://github.com/microsoft/fluentui/pull/36360)

9. **#36359 — inline SWC 変換による opt-in ESM-first ビルド後処理**  
ESM を opt-in したパッケージ向けに、CommonJS 出力を `.cjs`、型定義を `.d.cts` として扱う基盤を追加する PR です。現行の CommonJS パッケージでは何もしない設計です。[PR を見る](https://github.com/microsoft/fluentui/pull/36359)

---

## .NET Aspire

.NET Aspire は、分散アプリケーションの構築・実行・観測を支援する .NET の開発スタックです。API の成熟、Dashboard と CLI、セキュリティ、Kubernetes 連携にまたがる 25 件がマージされています。

### API とホスティング

1. **#19007 — 1 年経過した experimental Aspire API を正式化**  
1 年が経過したことを基準に、独立して正式化できる experimental API の作業を始める PR です。若い experimental 依存関係を露出させない形で、該当 API の experimental マークを外します。[PR を見る](https://github.com/dotnet/aspire/pull/19007)

3. **#18998 — `SubmitAsync` のトークンから空白を除去**  
`SubmitAsync` メソッド内のトークン文字列の空白をトリミングする修正です。変更対象は PR タイトルで明示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18998)

5. **#18992 — `TerminalOptions` の columns と rows を正数として検証**  
`TerminalOptions` の columns と rows に対し、正の値を求める検証を追加する変更です。不正な端末サイズ指定を扱う API の入力検証です。[PR を見る](https://github.com/dotnet/aspire/pull/18992)

7. **#18991 — 誤解を招く `TerminalOptions.Shell` の no-op を削除** **破壊的変更**  
実際にはシェル選択へ影響しない experimental API の `TerminalOptions.Shell` を削除する PR です。このプロパティを設定している AppHost では、代入を削除する必要があります。[PR を見る](https://github.com/dotnet/aspire/pull/18991)

9. **#18978 — terminal API surface を修正**  
terminal API surface を修正する PR です。変更の主題はタイトルで示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18978)

### Dashboard と開発体験

1. **#18989 — Dashboard メニューを遅延初期化**  
Dashboard のメニュー初期化を必要になるまで遅らせる変更です。対象は PR タイトルで明示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18989)

3. **#18987 — デバッグなしの AppHost 起動に run の表現を使用**  
デバッグなしで AppHost を起動するケースの表現を「run」に統一する変更です。対象は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18987)

5. **#18985 — Dashboard バンドル準備中のステータスを表示**  
Dashboard バンドルの準備中に状態を表示する変更です。待機中の状況をユーザーへ示すことが主題です。[PR を見る](https://github.com/dotnet/aspire/pull/18985)

7. **#18973 — VS Code で runtime が unhealthy のリソースを警告表示**  
ランタイムが unhealthy と判定したリソースを VS Code 上で警告として表示する変更です。リソース状態の見え方を改善します。[PR を見る](https://github.com/dotnet/aspire/pull/18973)

9. **#18943 — Dashboard のデザイントークン、タイポグラフィ、UX、axe テストを刷新**  
Dashboard のデザイントークンとタイポグラフィを見直し、UX の調整と `axe` によるアクセシビリティテストを含める PR です。視覚面とアクセシビリティの両方を対象にしています。[PR を見る](https://github.com/dotnet/aspire/pull/18943)

### CLI、配布、拡張機能

1. **#18980 — hosted agent の ATS 互換性を復元**  
hosted agent の ATS 互換性を復元する修正です。対象は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18980)

3. **#18968 — CLI パッケージのメタデータ事前取得を opt-in 化**  
CLI パッケージのメタデータを事前取得する挙動を opt-in にする変更です。既定では自動的に行わない形にします。[PR を見る](https://github.com/dotnet/aspire/pull/18968)

5. **#18940 — Windows での global-tool Aspire CLI 検出を修正**  
Windows で global tool として入れた Aspire CLI を検出する処理の修正です。対象プラットフォームと課題はタイトルで明示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18940)

7. **#18621 — Path ツリー項目のクリックで AppHost パスをクリップボードへコピー**  
Path ツリー項目をクリックしたとき、AppHost のパスをクリップボードへコピーする変更です。関連する issue #18578 を解決します。[PR を見る](https://github.com/dotnet/aspire/pull/18621)

9. **#18606 — npm パッケージ README に CLI リリースノートを表示**  
npm パッケージの README で CLI のリリースノートを見せる変更です。配布物の README を対象にしています。[PR を見る](https://github.com/dotnet/aspire/pull/18606)

### セキュリティと依存関係

1. **#18997 — npm のサプライチェーン侵害後に `cacheable-request` を固定**  
npm のサプライチェーン侵害を受け、`cacheable-request` のバージョンを固定する変更です。依存関係の固定が主題です。[PR を見る](https://github.com/dotnet/aspire/pull/18997)

3. **#18995 — npm／pip／yarn の低リスク依存関係セキュリティ修正を統合**  
Aspire の低リスクな依存関係セキュリティ修正をまとめる auto-sec PR です。対象は npm、pip、yarn として示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18995)

5. **#18858 — JavaScript テンプレートの依存関係セキュリティ修正を統合**  
Aspire の JavaScript テンプレートに関する依存関係セキュリティ修正をまとめる auto-sec PR です。変更対象は PR タイトルで明示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18858)

7. **#18605 — VS Code 拡張機能に secure publish を使用**  
VS Code 拡張機能の公開で secure publish を使う変更です。公開経路の扱いを対象にしています。[PR を見る](https://github.com/dotnet/aspire/pull/18605)

### ホスティング基盤、クラウド連携、内部構造

1. **#18977 — `CommandProgressOptions` を sealed に変更**  
`CommandProgressOptions` を sealed にする変更です。対象の型と変更内容は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18977)

3. **#18974 — interaction API のドキュメントを明確化**  
interaction API のドキュメントを明確にする PR です。主題は API ドキュメントの説明改善です。[PR を見る](https://github.com/dotnet/aspire/pull/18974)

5. **#18918 — `Aspire.Hosting.Dotnet` を `Aspire.Hosting` の内部実装から分離**  
`Aspire.Hosting.Dotnet` と `Aspire.Hosting` の内部実装の結び付きを分離する変更です。対象のアセンブリはタイトルで明示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18918)

7. **#18895 — Azure SQL の managed identity プロビジョニングスクリプトを修正**  
`az` 14.0 の deployment image 上で動作する Azure SQL の managed identity プロビジョニングスクリプトを修正する PR です。対象環境がタイトルで示されています。[PR を見る](https://github.com/dotnet/aspire/pull/18895)

9. **#18443 — 拡張機能での AppHost 検出を incremental 化**  
拡張機能による AppHost 検出をインクリメンタルにする変更です。検出方法を対象にしています。[PR を見る](https://github.com/dotnet/aspire/pull/18443)

11. **#16555 — Kubernetes projected volumes をサポート**  
Kubernetes の projected volumes を扱えるようにする PR です。複数の secret を同じフォルダーへマウントする用途が動機として挙げられています。[PR を見る](https://github.com/dotnet/aspire/pull/16555)

---

## .NET Runtime

.NET Runtime は、.NET のランタイム、ライブラリ、JIT、各プラットフォーム向けの実行基盤を管理するリポジトリです。今回の 43 件はテスト基盤、JIT、TLS、cDAC、Mono、リリースブランチまで広く分布しています。

### ブラウザー、モバイル、テスト基盤

1. **#131883 — CoreCLR browser の R2R カバレッジを復元し、未対応の priority-1 テストを制御**  
CoreCLR browser 向けの R2R カバレッジを戻し、未対応の priority-1 テストの実行を制御する変更です。対象は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131883)

3. **#131864 — Helix Job Monitor を更新**  
Helix Job Monitor を更新する PR です。更新対象はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131864)

5. **#131863 — CoreCLR browser-wasm の native relink headers を runtime pack に含める**  
browser-wasm 向け CoreCLR の native relink headers を runtime pack に含める変更です。対象は browser と runtime pack です。[PR を見る](https://github.com/dotnet/runtime/pull/131863)

7. **#131857 — TLS セッションテストで受け入れ済みソケットを維持**  
TlsSession テストで accept 済みのソケットを保持する修正です。対象はテストのソケット寿命です。[PR を見る](https://github.com/dotnet/runtime/pull/131857)

9. **#131844 — FeatureMultithreading の props を統合し、テストスキップに `ConditionalFact` を使用**  
`clr.featuremultithreading.props` を `clr.featuredefines.props` へ統合する変更です。FeatureMultithreading のテストスキップには `ConditionalFact` を使います。[PR を見る](https://github.com/dotnet/runtime/pull/131844)

11. **#131829 — crossgen テストから rundownvalidation を除外**  
crossgen テストで rundownvalidation を除外する PR です。対象は PR タイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131829)

13. **#131816 — interpreter 下で GetGeneration テストを無効化**  
interpreter 環境では GetGeneration テストを `InterpreterIncompatible` として無効化する変更です。対象テストと条件はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131816)

15. **#131787 — `runtime.yml` で iOS device の実行系を復元**  
mobile 向けに iOS device の実行系を `runtime.yml` へ戻す変更です。対象ファイルと環境がタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131787)

### JIT とメモリ

1. **#131845 — `BufferBuilder.EnsureBuffer` のサイズ計算における整数オーバーフローを修正**  
`BufferBuilder.EnsureBuffer` のサイズ計算で起きる整数オーバーフローを修正する PR です。対象メソッドと不具合はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131845)

3. **#131830 — `thread_final_regions` で空き領域を得られない場合の処理を修正**  
`thread_final_regions` が空き領域を取得できないケースの扱いを修正する変更です。対象は PR タイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131830)

5. **#131827 — `GCMemoryInfoData` で inline arrays を使用**  
`GCMemoryInfoData` に inline arrays を用いる変更です。対象の型はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131827)

7. **#131810 — release/10.0 の JIT で複数 successor を持つ alloc block の CEA cloning を中止**  
release/10.0 ブランチの JIT で、alloc block が複数の successor を持つ場合は CEA cloning から離脱する変更です。対象条件はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131810)

9. **#131781 — release/10.0 の JIT で反転した bit table を使う bit-test switch lowering を修正**  
release/10.0 の JIT における bit-test switch lowering を修正する PR です。反転した bit table を使う場合を対象にしています。[PR を見る](https://github.com/dotnet/runtime/pull/131781)

11. **#131762 — JIT の CEA cloning で複数 successor を持つ alloc block を回避**  
JIT で、alloc block が複数の successor を持つ場合の CEA cloning を回避する変更です。対象条件はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131762)

13. **#131754 — JIT の indirect call transformer で不要な引数 spill を回避**  
JIT の indirect call transformer が不要な引数 spill をしないようにする修正です。対象はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131754)

15. **#131750 — 大きな要素と struct 要素向けに多次元配列 intrinsic を拡張**  
JIT の多次元配列 intrinsic を、大きな要素と struct 要素に対応させる変更です。対象範囲はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131750)

17. **#131729 — large array テストを排他的に実行**  
large array テストを排他的に実行する変更です。対象テストの実行方法を扱います。[PR を見る](https://github.com/dotnet/runtime/pull/131729)

### TLS、ソケット、ライブラリ

1. **#131819 — watchdog の子プロセス終了ステータス確認を改善**  
watchdog が確認する子プロセスの終了ステータスを改善する PR です。対象は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131819)

3. **#131808 — `CreateThread` 失敗時の誤った確認を修正**  
`CreateThread` の失敗確認にある誤りを修正する変更です。対象 API と課題はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131808)

5. **#131807 — x64 Unix 向け `VOLATILE_MEMORY_BARRIER` を変更**  
x64 Unix 向けの `VOLATILE_MEMORY_BARRIER` を変更する PR です。対象プラットフォームと要素はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131807)

7. **#131791 — tar エントリデータコピー時の無制限な `MemoryStream` 確保を回避**  
tar エントリのデータをコピーする際、`MemoryStream` が無制限に確保されることを避ける修正です。対象のメモリ確保はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131791)

9. **#131759 — ソケットに束縛された TLS セッションで write buffer の移動を許可**  
ソケットに束縛された TLS セッションで、移動可能な書き込みバッファーを受け入れる変更です。対象は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131759)

11. **#131758 — 所有していない IntermediateCertificates を `SslAuthenticationOptions` が破棄する問題を修正**  
`SslAuthenticationOptions` が呼び出し元が所有する IntermediateCertificates を誤って破棄する問題を修正します。`UpdateOptions` を複数回呼ぶ場合の所有権を扱う内容です。[PR を見る](https://github.com/dotnet/runtime/pull/131758)

13. **#131755 — 特定の `SslStreamServer` テストで TLS resume を無効化**  
`SslStreamServer_RejectsClientCert_ClientObservesAlert` テストで TLS resume を無効化する変更です。対象テストはタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131755)

15. **#131728 — `MemoryCache` の並行性テストを修正**  
`MemoryCache` の並行性テストを修正する PR です。対象はテストです。[PR を見る](https://github.com/dotnet/runtime/pull/131728)

### cDAC、デバッグ、契約

1. **#131837 — cDAC のメソッド列挙で legacy AppDomain を unwrap**  
cDAC のメソッド列挙で legacy AppDomain を unwrap する変更です。対象のデバッグデータアクセス処理はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131837)

3. **#131836 — cDAC で基本オブジェクト型の入力を保持**  
cDAC が基本オブジェクト型の入力を保持するようにする変更です。対象は PR タイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131836)

5. **#131825 — `MSCORDBI_LINKS_PRIVATE_PAL` 定義付きの `cordbdi_universal` static library を追加**  
`MSCORDBI_LINKS_PRIVATE_PAL` を定義した `cordbdi_universal` static library を追加する PR です。ライブラリと定義はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131825)

7. **#131797 — コンパイル時に global classified を指定した場合の union ambiguity warning を抑制**  
global classified がコンパイル時に指定された場合の union ambiguity warning を抑制する変更です。対象条件はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131797)

9. **#131796 — s390x 向け in-process crash reporter を追加**  
s390x 向けの in-process crash reporter を追加する PR です。対象アーキテクチャはタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131796)

11. **#131790 — 開発専用の contract versions を削除**  
未サポートの開発ビルドでだけ使われた contract versions を整理して削除する変更です。契約を固定する準備として、開発期間中の複数バージョンを簡素化します。[PR を見る](https://github.com/dotnet/runtime/pull/131790)

13. **#131780 — cDAC の runtime version information を追加**  
cDAC に runtime version information を追加する変更です。対象はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131780)

15. **#131777 — cDAC の `IterateMethodInstances` legacy fallback を削除**  
cDAC の `IterateMethodInstances` にある legacy fallback を削除する PR です。対象 API と削除内容はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131777)

17. **#131719 — unsafe-v2 でも安全な CoreLib メンバーを監査**  
`unsafe-v2` 下でも安全に残せる CoreLib メンバーを監査する変更です。対象範囲は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131719)

19. **#131690 — cDAC の `IXCLRDataValue` API を実装**  
cDAC 向けに `IXCLRDataValue` API を実装する PR です。対象 API はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131690)

21. **#131753 — EnC delta が record を追加したとき metadata table lookup hash を無効化**  
EnC delta が record を追加する場合、metadata table lookup hash を無効化する変更です。対象条件と処理は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131753)

### Mono、WASM、プロファイリング

1. **#131786 — clr-interp で interpreter frame data への DAC アクセスを修正**  
clr-interp における interpreter frame data への DAC アクセスを修正します。対象はタイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131786)

3. **#131739 — Mono で generic type にネストした enum の `InvalidCastException` を修正**  
Mono 上で generic type にネストした enum が `InvalidCastException` になる問題を修正します。対象条件はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131739)

5. **#131688 — JIT で冗長な wasm Virtual IP 更新を省略**  
JIT が不要な wasm Virtual IP 更新を省略する変更です。対象は PR タイトルで示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131688)

7. **#131681 — Runtime Async メソッド本体の profiler ReJIT を修正**  
Runtime Async method bodies に対する profiler の ReJIT を修正する PR です。対象はタイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131681)

### リリースと保守

1. **#131865 — ドキュメント内の壊れた相対リンクを修正**  
ドキュメントの壊れた相対リンクを修正する PR です。対象はリンクです。[PR を見る](https://github.com/dotnet/runtime/pull/131865)

3. **#131795 — release/10.0 の依存関係を `dotnet/hotreload-utils` と `dotnet/runtime-assets` から更新**  
release/10.0 ブランチの依存関係を更新する変更です。更新元は `dotnet/hotreload-utils` と `dotnet/runtime-assets` です。[PR を見る](https://github.com/dotnet/runtime/pull/131795)

5. **#131775 — code review skill に GPT-5.3-codex を提案しない**  
code review skill で GPT-5.3-codex を提案しない変更です。対象は PR タイトルで明示されています。[PR を見る](https://github.com/dotnet/runtime/pull/131775)

---

## Microsoft Agent Framework

Microsoft Agent Framework は、.NET と Python のエージェント、ワークフロー、プロトコル連携を扱うリポジトリです。今回はバージョン更新に加え、A2A、AG-UI、ツール承認、コンテキスト圧縮の堅牢性を高める変更が加わっています。

### .NET の実行、A2A、ワークフロー

1. **#7514 — .NET リリース 1.17.0 向けにバージョンを更新**  
.NET の 1.17.0 リリースに向けたバージョン更新です。変更の目的は PR 本文で明示されています。[PR を見る](https://github.com/microsoft/agent-framework/pull/7514)

3. **#7505 — DevUI proxy validation 用の CodeQL suppression を追加**  
DevUI aggregator proxy に inline の CodeQL suppression コメントを追加する変更です。proxy target は host、scheme、port を検証する `ValidateProxyTarget` により制約されると説明されています。[PR を見る](https://github.com/microsoft/agent-framework/pull/7505)

5. **#7504 — experimental A2A task store に isolation key scoping を拡張**  
`ITaskStore` を呼び出し元ごとのキーでスコープする `IsolationKeyScopedTaskStore` を追加する変更です。`SessionIsolationKeyProvider` が登録されている場合、`CreateA2AServer` は task store を自動的にラップします。[PR を見る](https://github.com/microsoft/agent-framework/pull/7504)

7. **#7497 — agent がエラーを返したとき declarative workflow を失敗させる**  
agent の失敗レスポンスを workflow が成功として扱い、空の完了メッセージになる問題を修正します。失敗した run を error content に変換し、エラーを含むレスポンスで action を停止します。[PR を見る](https://github.com/microsoft/agent-framework/pull/7497)

### Python、AG-UI、ツール処理

1. **#7480 — AG-UI の approval resume をプロトコル境界で修正**  
AG-UI の approval resume で、終端済みの function result が再生される問題をプロトコル境界で修正します。pending registry との照合により、古い・未知・別 call の承認レスポンスを拒否します。[PR を見る](https://github.com/microsoft/agent-framework/pull/7480)

3. **#7474 — .NET の tool-approval auto-approval loop に上限を設定**  
自動承認されたツール要求で内側の agent を再実行し続ける無制限ループを制限する変更です。既定値 10 の `MaxAutoApprovalIterations` を導入し、上限到達後は残る承認要求を呼び出し元へ提示します。[PR を見る](https://github.com/microsoft/agent-framework/pull/7474)

5. **#7430 — Python の AG-UI conversation correlation を run をまたいで修正**  
AG-UI の `threadId` と `runId` を、プロバイダーが返す識別子で置き換えないようにする変更です。安定した thread を telemetry と snapshot に引き継ぎ、各 run の trace は分けます。[PR を見る](https://github.com/microsoft/agent-framework/pull/7430)

7. **#7396 — Python の tool result compaction summaries に上限を設定**  
大きな tool result が要約メッセージへそのまま入ることで、圧縮後も大きな payload が残る問題を修正します。含める対象のメッセージだけから要約を作り、生成する要約テキストを切り詰めます。[PR を見る](https://github.com/microsoft/agent-framework/pull/7396)

---

## StackChan

StackChan は、M5Stack を使ったコミュニケーションロボット「StackChan」のリポジトリです。

集計期間内にマージされた PR はありませんでした。

---

## まとめ

- **Fluent UI Blazor**: 5 件。FluentMenu の遅延描画、DataGrid の属性化、コンポーネントのアクセシビリティ改善が中心でした。
- **Fluent UI**: 10 件。ヘッドレス API のバンドル削減と、ESM-first 移行を opt-in で進める開発基盤の変更がありました。
- **.NET Aspire**: 25 件。API の成熟度向上、Dashboard と CLI、依存関係のセキュリティ、Kubernetes の projected volumes まで幅広く含みます。
- **.NET Runtime**: 43 件。テスト基盤、JIT、TLS、cDAC、Mono、リリースと保守にまたがる更新です。
- **Microsoft Agent Framework**: 8 件。A2A、AG-UI、ツール承認、ワークフローのエラー処理を強化しています。
- **StackChan**: 0 件。集計期間内にマージされた PR はありませんでした。