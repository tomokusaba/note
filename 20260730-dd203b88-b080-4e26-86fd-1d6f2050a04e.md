# 2026-07-30 .NET と GitHub の注目アップデート

.NET で MCP サーバーを運用する開発者と、GitHub の管理・セキュリティ・パッケージ公開を担うチームに影響する公式更新をまとめます。

## Developer Blogs

### MCP C# SDK 2.0 が登場

[Announcing v2.0 of the official MCP C# SDK](https://devblogs.microsoft.com/dotnet/announcing-v20-of-the-official-mcp-csharp-sdk/?WT.mc_id=DT-MVP-5004827) で、公式 MCP C# SDK の v2.0 が発表されました。MCP 仕様の 2026-07-28 改訂に対応し、HTTP トランスポートはステートレスが既定になります。

これにより、リクエストは特定のサーバーインスタンスに固定されず、ロードバランサー配下や複数インスタンスで扱いやすくなります。対話的な処理には、セッションを持続させずに入力を受け渡せる Multi Round-Trip Requests も導入されました。対話ツールを移行する場合は、利用するクライアントがこの経路に対応しているかを確認します。

既存の v1 コードとの後方互換性は維持されます。一方で、セッションに依存した機能や旧来の SSE エンドポイントを使っている場合は、v2 へ移行する前にステートフル構成を明示的に有効化する必要があるかを確認します。

## GitHub Changelog

### Copilot の新しい一般提供モデルを既定で有効化するポリシー

[Default model enablement for Copilot Business and Enterprise](https://github.blog/changelog/2026-07-29-default-model-enablement-for-copilot-business-and-enterprise/) により、Copilot Business と Copilot Enterprise では、一般提供になったモデルを既定で有効にする組織・エンタープライズ向けポリシーが追加されました。

ポリシーは現在設定できますが、モデルの提供状況に反映されるのは 8 月 26 日からです。既定では有効であり、個別に設定していないモデルはポリシーを継承して利用可能になります。新しいモデルを都度承認したい組織は、それまでにポリシーを無効化し、現在のモデル設定を確認する必要があります。個別に有効・無効を設定済みのモデルは維持されます。オープンウェイトモデルと GitHub のデータ保持契約の対象外モデルは、この既定有効化の対象外です。

### CodeQL 2.26.1 が GitHub.com のコードスキャンに反映

[CodeQL 2.26.1 improves analysis accuracy and framework coverage](https://github.blog/changelog/2026-07-29-codeql-2-26-1-improves-analysis-accuracy-and-framework-coverage/) では、Go、Java/Kotlin、JavaScript/TypeScript のフレームワーク対応を拡げ、Rust の誤検知を減らす変更が案内されています。

たとえば、Go の `log/slog` モデリング強化はログインジェクションと平文ログのクエリ対象を広げます。Java/Kotlin では Spring WebFlux の URI 指定が SSRF のシンクとして扱われるようになるため、新しい有効なアラートが現れる可能性があります。GitHub.com の Code Scanning 利用者には自動適用されるため、更新後のアラートをトリアージする運用を確認します。

### Copilot app の利用状況を既存のメトリクスで分析可能に

[GitHub Copilot app usage metrics now expand across report rollups](https://github.blog/changelog/2026-07-28-github-copilot-app-usage-metrics-now-expand-across-report-rollups/) により、Copilot usage metrics API で Copilot app の利用状況をユーザー別、機能別、モデル別、言語別の集計に含められるようになりました。

ユーザー別レポートには `used_copilot_app` と `totals_by_copilot_app` が追加され、セッション数、リクエスト数、プロンプト数、トークン利用量を確認できます。コード生成や受け入れ、追加・削除行、日次アクティブユーザーにも Copilot app の活動が反映されます。利用状況ポリシーを有効化済みの組織では、IDE、チャット、コードレビュー、コーディングエージェントと同じ指標で比較できるようになります。

### npm 公開時のマルウェアスキャンと二重用途パッケージの要件

[npm publish-time malware scanning and dual-use metadata](https://github.blog/changelog/2026-07-28-npm-publish-time-malware-scanning-and-dual-use-metadata/) により、npm に公開するパッケージはインストール可能になる前に自動スキャンされます。通常は約 5 分、混雑時などには 15 分以上の公開待ちが発生し得るため、公開直後のインストールを前提にした自動化は待機を許容するように見直す必要があります。

セキュリティ関連の機能を持つ二重用途パッケージでは、`package.json` の `contentPolicy` と、用途を説明するルート直下の `DISCLOSURE` ファイルが新たに求められます。さらに 2FA を強制する公開方法が必要で、2FA をバイパスするトークンによる直接公開はできません。一度宣言した `contentPolicy` と `DISCLOSURE` は後続版でも削除できません。該当パッケージは段階的に適用されるため、メンテナーは公開フローとパッケージ内容を確認します。

## まとめ

- MCP C# SDK 2.0 は、ステートレスな HTTP 運用と対話的なツール呼び出しを両立しつつ、v1 との後方互換性を維持します。
- Copilot の組織管理では、新モデルの既定提供ポリシーと、Copilot app を含む利用分析の見直しが重要です。
- Code Scanning と npm 公開では、検出範囲・公開フローの変化に合わせてアラート対応と自動化を確認します。
