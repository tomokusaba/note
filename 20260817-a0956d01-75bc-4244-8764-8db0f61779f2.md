# 2026年8月17日版：Fluent UI、Aspire、.NET Runtime、Agent Framework の注目マージ PR まとめ

## はじめに

対象リポジトリで直近にマージされた PR を、変更の背景と利用者への影響が分かる形でまとめます。Fluent UI、Aspire、.NET Runtime、Microsoft Agent Framework を追っている方が、短時間で優先的に確認すべき変更をつかむための記事です。

- **確認日時（JST）**：2026-08-17 06:01 JST
- **集計範囲（UTC、境界を含む）**：`mergedAt >= 2026-08-15T00:00:00Z` かつ `mergedAt <= 2026-08-16T21:01:03Z`
- **集計方法**：各リポジトリでマージ済み PR を取得した後、`mergedAt` を上記の UTC 範囲でローカルに絞り込みました。

## 対象リポジトリ一覧

- **microsoft/fluentui-blazor**：1 件（#5150）
- **microsoft/fluentui**：0 件
- **microsoft/aspire**：11 件（#19374、#19411、#19373、#19344、#19412、#19375、#19415、#19245、#19399、#19401、#19417）
- **dotnet/runtime**：7 件（#132302、#132248、#132033、#132345、#132342、#130835、#131867）
- **microsoft/agent-framework**：3 件（#7652、#7655、#7486）
- **m5stack/StackChan**：0 件

---

## microsoft/fluentui-blazor

1. **#5150 `[dev-v5] Implement immediate input handling with improved focus management`** — [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5150)  
   遅延付きの即時入力で、通信遅延時に古いサーバー側の値がブラウザーの入力を上書きしてしまう問題に対応する変更です。入力をデバウンスし、最新のイベントだけを順番に確定させるとともに、フォーカス中は描画値を固定するため、Blazor Server など高遅延の環境でも入力内容が最後のキー入力へ収束しやすくなります。

## microsoft/fluentui

集計期間内にマージされた PR はありませんでした。

## microsoft/aspire

11 件あるため、Kubernetes／デプロイ、開発体験、依存関係・リリースに分けて見ます。

### Kubernetes とデプロイ

1. **#19374 `Set a default fsGroup for Kubernetes persistent volumes`** — [PR を見る](https://github.com/microsoft/aspire/pull/19374)  
   非 root ユーザーで動くワークロードが、正常にマウントされた永続ボリュームへ書き込めずに失敗する背景を解消する変更です。`WithPersistentVolume(...)` に既定の `fsGroup: 2000` と `OnRootMismatch` を付与し、必要な環境では既存のカスタマイズコールバックで上書きまたは無効化できます。

3. **#19412 `Fix deployment E2E test matrix enumeration`** — [PR を見る](https://github.com/microsoft/aspire/pull/19412)  
   `AksPersistentVolumeDeploymentTests` の意図しない `Partition` trait により、デプロイ E2E テストがクラスごとのジョブではなく 2 つのバケットへ縮退していた問題を直します。trait を取り除くことで、隔離・outerloop の除外後も 42 個のアクティブなクラス単位ジョブが生成され、テストの粒度が回復します。

5. **#19401 `[release/13.5] Set a default fsGroup for Kubernetes persistent volumes`** — [PR を見る](https://github.com/microsoft/aspire/pull/19401)  
   release/13.5 向けのボットによる**バックポート**で、#19374 の永続ボリューム書き込み権限の改善を取り込みます。新しい first-class 永続ボリューム経路だけに限定され、既定値は上書き・削除できるため、リリース利用者の非 root ワークロードが書き込めない問題を避けやすくなります。

### 開発体験と診断

1. **#19344 `Handle SIGTERM in terminal host and GC orphaned sockets (#19302)`** — [PR を見る](https://github.com/microsoft/aspire/pull/19344)  
   `WithTerminal()` の端末ホストが SIGTERM や AppHost の予期しない終了後に Unix ドメインソケットを残す問題を扱います。共有の終了処理、親プロセスの監視、実行ごとのランダム ID、孤児ソケットの安全な回収を加え、並行実行や PID 再利用を含む状況で後続の実行に影響しにくくします。

3. **#19375 `Surface ASPIRE010 help link inline in warning text`** — [PR を見る](https://github.com/microsoft/aspire/pull/19375)  
   `AspireUseCliBundle=false` 時の MSBuild 警告 `ASPIRE010` は、従来の `HelpLink` メタデータがターミナル出力に表示されず、調査先を見つけにくい状態でした。警告本文へ診断 URL を直接含めることで、コンソールでビルドする利用者もその場で詳細へ進めます。

5. **#19415 `[release/13.5] Surface ASPIRE010 help link inline in warning text`** — [PR を見る](https://github.com/microsoft/aspire/pull/19415)  
   release/13.5 向けのボットによる**バックポート**で、#19375 の警告本文へのヘルプリンク追加を取り込みます。コンソール出力だけを見ている利用者にも `ASPIRE010` の調査先を示せるようになります。

7. **#19399 `[release/13.5] Report actionable errors when DotnetProjectResource is published`** — [PR を見る](https://github.com/microsoft/aspire/pull/19399)  
   release/13.5 向けのボットによる**バックポート**で、`DotnetProjectResource` の発行が未対応である場合に対応方法を示すエラーを表示する #19190 の変更を取り込みます。リリース系列で問題に遭遇したとき、次の対応を判断しやすいエラー情報を受け取ることが狙いです。

### 依存関係とリリース

1. **#19411 `[Automated] Update Microsoft Foundry Models`** — [PR を見る](https://github.com/microsoft/aspire/pull/19411)  
   Microsoft Foundry のモデル記述子を自動更新するボットによる変更です。生成済みの `FoundryModel.Generated.cs` を更新し、Aspire 側のモデル情報を追従させます。

3. **#19373 `Update gRPC packages to 2.83.0`** — [PR を見る](https://github.com/microsoft/aspire/pull/19373)  
   `Grpc.AspNetCore`、`Grpc.Net.ClientFactory`、`Grpc.Tools` を 2.83.0 へ更新し、Linux ARM64 での `protoc` クラッシュに対応します。古いプロジェクト単位の上書きを外し、protobuf を実際にコンパイルするクライアント専用プロジェクトだけが中央管理された `Grpc.Tools` を直接参照する形へ整理します。

5. **#19245 `Update dependencies from latest .NET Servicing and bump SDK to 10.0.400`** — [PR を見る](https://github.com/microsoft/aspire/pull/19245)  
   Patch Tuesday のサービス更新に合わせ、.NET 10 Preview、.NET 9 Current、.NET 8 LTS の対象パッケージと SDK を更新する変更です。SDK は `10.0.400` となり、依存関係の復元は新しいサービス版に揃います。

7. **#19417 `[release/13.5] Fix npm registry hard-coded to internal dnceng feed (#19370)`** — [PR を見る](https://github.com/microsoft/aspire/pull/19417)  
   release/13.5 で内部の dnceng フィードを既定にしたことにより、未キャッシュの依存 tarball で 401 が返り `aspire agent init` が失敗する回帰を修正します。スターターテンプレートと生成される JavaScript 用 Dockerfile は公開 npm レジストリを既定にしつつ、`NPM_REGISTRY` による上書きは維持します。

## dotnet/runtime

7 件あるため、実行基盤、相互運用の性能、安定性・テストに分類します。

### 実行基盤と ABI

1. **#132302 `Add locking for cDAC APIs`** — [PR を見る](https://github.com/dotnet/runtime/pull/132302)  
   同じ `Target` を共有する cDAC API が同時に処理へ入らないよう、ロックを追加する変更です。共有対象への並行アクセスによる競合を抑え、cDAC 操作の安全性を高めます。

3. **#132248 `[wasm] Encode struct alignment in thunk signatures`** — [PR を見る](https://github.com/dotnet/runtime/pull/132248)  
   Wasm で値渡しする 16 バイト境界の構造体を正しく扱うため、thunk 署名にアラインメントを符号化します。8 バイトと 16 バイトの構造体を区別し、crossgen と Wasm ReadyToRun のテストで ABI 上の扱いを検証します。

5. **#132345 `Restore Windows Arm64 support for CPUs without LSE`** — [PR を見る](https://github.com/dotnet/runtime/pull/132345)  
   LSE を必須ではなくオプションの CPU 機能として扱い、LSE を持たない NXP i.MX 8 などでも Windows Arm64 の JIT と NativeAOT プロセスを起動可能にします。ReadyToRun の要件は Armv8.2-A＋RCPC のままなので、該当機では R2R を使わず JIT へフォールバックします。

### 相互運用の性能

1. **#132033 `Partition the ComWrappers RCW cache into per-processor buckets`** — [PR を見る](https://github.com/dotnet/runtime/pull/132033)  
   `ComWrappers` の RCW キャッシュが単一ロックだったため、ネイティブからマネージドへの遷移とファイナライザーによる削除が競合していた背景があります。COM ポインターをハッシュしてプロセッサ数に応じたバケットへ分割し、異なる COM オブジェクトの参照が同じロックを奪い合いにくくします。

### 安定性とテスト

1. **#132342 `Skip GitHub_130545 on NativeAOT`** — [PR を見る](https://github.com/dotnet/runtime/pull/132342)  
   Mono WASM AOT 向けの回帰テストが、NativeAOT ではジェネリック制約メタデータの解決時に `TypeLoadException` となるため、一時的に NativeAOT だけでスキップします。CoreCLR と Mono でのカバレッジは維持し、根本の NativeAOT リフレクション メタデータ問題は別の issue で追跡します。

3. **#130835 `Buffer consecutive text nodes when loading XContainer to avoid O(n^2)`** — [PR を見る](https://github.com/dotnet/runtime/pull/130835)  
   XML 読み込み時に小さな連続テキストノードを個別に不変文字列へ連結すると、処理量が O(n²) になり、逆シリアル化が極端に遅くなる問題を改善します。連続したテキストを `StringBuilder` にためて一度に具体化し、生成されるノードツリーを保ったまま線形処理へ近づけます。

5. **#131867 `Fix macOS crash in NetworkChange when the run loop thread already exited`** — [PR を見る](https://github.com/dotnet/runtime/pull/131867)  
   macOS のネットワーク変更監視で run loop スレッドが既に終了した後、最後の購読解除時に `null` の run loop を参照してプロセスがクラッシュする問題を修正します。`s_runLoop` がゼロなら安全に待機・復帰する分岐を加え、スリープ復帰や `configd` の再起動後の終了処理でのクラッシュを防ぎます。

## microsoft/agent-framework

1. **#7652 `Python: Fix duplicate streamed tool calls in DevUI`** — [PR を見る](https://github.com/microsoft/agent-framework/pull/7652)  
   プロバイダーが同じ関数呼び出し ID と関数名を引数チャンクごとに含めると、DevUI が 1 回のツール実行を複数回表示してしまう問題を直します。最初に観測した ID だけで `response.output_item.added` を出し、後続の引数差分は保持するため、表示は 1 つのツール呼び出しにまとまります。

3. **#7655 `Python: Fix AG-UI url source dropping attachments when the URL is in source.value`** — [PR を見る](https://github.com/microsoft/agent-framework/pull/7655)  
   AG-UI 仕様の URL 型ソースは URL を `source.value` に置くのに、アダプターが `source.url`／`source.uri` だけを読んで添付を黙って捨てていた問題です。`source.value` を優先して読み、従来の非標準形式もフォールバックとして残すため、仕様準拠の添付が `Content.from_uri(...)` へ変換されます。

5. **#7486 `Python: Enhance _OutputItemTracker to prevent duplicate function call streaming`** — [PR を見る](https://github.com/microsoft/agent-framework/pull/7486)  
   宣言だけのツールをストリーミングすると、メタデータだけの 2 つ目の関数呼び出しまで利用者へ流れてしまう不具合に対応します。`_OutputItemTracker` を強化し、同じツール呼び出しが重複して表示されないようにします。

## m5stack/StackChan

集計期間内にマージされた PR はありませんでした。

---

## まとめ

- 集計範囲内で確認できた PR は**22 件**でした。内訳は Aspire 11 件、.NET Runtime 7 件、Agent Framework 3 件、Fluent UI Blazor 1 件です。
- Aspire は Kubernetes 永続ボリュームの権限、端末ホストの後始末、公開 npm レジストリへの復帰が利用時の影響が大きい変更です。
- .NET Runtime は Wasm ABI、Windows Arm64 の互換性、COM 相互運用の競合低減、macOS のクラッシュ修正が中心でした。
- Agent Framework はストリーミング表示の重複と AG-UI 添付変換の不具合を修正しており、Python 版の UI・プロトコル連携を使う場合は確認価値があります。
- PR 本文で**破壊的変更**と明示された変更は、この集計対象にはありませんでした。
- 影響を受ける環境がある場合は、まず該当 PR のリンクを開き、続けて各プロダクトのリリースノートも確認してください。