# 2026-07-28 OSS PR サマリー — 主要リポジトリ動向

## はじめに

本記事は、**microsoft/fluentui-blazor**、**microsoft/fluentui**、**microsoft/aspire**、**dotnet/runtime**、**microsoft/agent-framework**、**m5stack/StackChan** の6リポジトリを対象に、直近にマージされた Pull Request（PR）を集計したサマリーです。

集計基準時刻は日本時間（JST）2026年7月28日6時02分41秒、対象期間は協定世界時（UTC）で **2026-07-26T00:00:00Z から 2026-07-27T21:02:41Z まで**（両端含む）としています。各 PR のタイトルと本文をもとに、変更の意図と影響を短くまとめました。

OSS の開発動向を短時間で把握したいエンジニア向けに、各 PR の意図と影響を整理しています。

## microsoft/fluentui-blazor

Blazor アプリケーション向けに Fluent UI デザインシステムのコンポーネント群を提供するリポジトリです。

集計期間内にマージされた PR はありませんでした。

## microsoft/fluentui

React および Web Components 実装を中心とした Fluent UI デザインシステムのモノレポです。今回はヘッドレスコンポーネントの API 整備と、Storybook・Web Components まわりの細かな修正が中心でした。

1. **PR #36452: fix(react-headless-components-preview): export useFieldContext and useFieldControlProps hooks** これまで `useFieldContext` と `useFieldControlProps` フックがヘッドレスパッケージから re-export されておらず、利用者が別パッケージへの依存を追加しないと使えない状態でした。本 PR でこれらのフックを直接 export し、ヘッドレスコンポーネントの利用者が余分な依存なしに扱えるようにしています。

[PR を見る](https://github.com/microsoft/fluentui/pull/36452)

3. **PR #36404: feat(babel-preset-storybook-full-source): add opt-in per-story fullSource slicing** Storybook の「Show code」パネル向けに story のソースを添付する Babel プリセットは、1ファイル内に複数の story が定義されている場合、最後の story にしかソースを付与できないという制約がありました。本 PR ではオプトインの `story` 単位モードを追加し、各 story が自分専用の実行可能なソース断片を持てるようにしています。

[PR を見る](https://github.com/microsoft/fluentui/pull/36404)

5. **PR #36443: fix(react-tag-picker): allow base states in render functions** TagPicker のヘッドレス版で、Button・Control・Group・Input の描画関数がベース状態の型を安全に受け取れず、型キャストを迫られていた課題に対応しています。関連する型を一貫して export することで、ヘッドレス利用者が描画関数を安全に再利用できるようにしました。

[PR を見る](https://github.com/microsoft/fluentui/pull/36443)

7. **PR #36395: fix(web-components): select a tab when click() is called on it** Web Components 版のタブでは、タブ選択処理がフォーカスイベントに依存していたため、`tabElement.click()` をプログラムから呼び出してもタブが選択されない不具合がありました。クリックイベントに対する処理を追加し、プログラムからのクリック操作でも意図どおりタブが選択されるよう修正しています。

[PR を見る](https://github.com/microsoft/fluentui/pull/36395)

## microsoft/aspire

.NET 向けのクラウドネイティブアプリケーション開発スタック「.NET Aspire」のリポジトリです。今回はダッシュボードの UX・セキュリティまわりの修正と、CLI・開発者ツールの改善に分けて紹介します。

### ダッシュボードの機能改善とバグ修正

1. **PR #18889: Fix disabled argument validation for interactive resource commands** インタラクティブなリソースコマンドで、無効化された動的入力が既定値を保持したまま扱われると `Argument is disabled.` エラーになる不具合がありました。無効入力チェックを非インタラクティブ経路に限定することで、インタラクティブなプロンプトが正しく動作するよう修正しています。

[PR を見る](https://github.com/microsoft/aspire/pull/18889)

3. **PR #18867: Default to Terminal view for live terminal resources in dashboard console logs** `WithTerminal()` を有効にしたリソースを選択した際、実行中であっても常に Console 表示が最初に開く点が使い勝手を損ねていました。リソースが実行中（Running）の場合はターミナルビューを既定表示にし、未起動・終了後はこれまでどおり Console を既定にするよう変更しています。

[PR を見る](https://github.com/microsoft/aspire/pull/18867)

5. **PR #18862: Restore focus after selecting dashboard menu items** ダッシュボードのボタンメニューから項目を選択すると、キーボードフォーカスがページ本体に残ってしまう問題が一部のメニューで再発していました。ボタン起点のメニューでフォーカス復元を既定動作にし、選択後にトリガーボタンへフォーカスが戻るよう統一しています。

[PR を見る](https://github.com/microsoft/aspire/pull/18862)

7. **PR #18866: Fix HTML injection in code block titles** Markdown 描画時にコードブロックのタイトルへ言語メタデータをそのまま書き込んでいたため、細工された値によって HTML インジェクションが起こり得るセキュリティ上の課題がありました。タイトル表示部分を HTML エンコードすることで、この注入経路を塞いでいます。

[PR を見る](https://github.com/microsoft/aspire/pull/18866)

9. **PR #18868: Improve AI agent observability text** AppHost とスタンドアロンダッシュボードにある AI エージェント向けダイアログの説明文を見直し、Aspire がエージェント型開発にどう価値をもたらすかをより明確に伝える文言へ更新しています。あわせてローカライズ用のソースファイルも再生成しています。

[PR を見る](https://github.com/microsoft/aspire/pull/18868)

11. **PR #18865: Reject histogram bucket count length changes** 同じ計装・次元系列内でヒストグラムのバケット数が変化するテレメトリを受け取ると、累積値の比較に失敗しダッシュボードの表示が壊れる不具合がありました。バケット数が変化したデータポイントを拒否するようにし、既存の正しいデータのみが表示され続けるよう修正しています。

[PR を見る](https://github.com/microsoft/aspire/pull/18865)

### CLI・開発者ツール関連

1. **PR #18839: Retry transient Windows bundle directory moves** Windows 環境ではウイルス対策ソフトやインデックス作成処理がバンドル展開直後のファイルを一時的に開くことがあり、これによりディレクトリの原子的な移動が失敗するケースがありました。バンドル昇格時のみ既存のリトライ機構を適用し、アクセス拒否系のエラーに限定して再試行することで、決定的な失敗はそのまま検知できるようにしています。

[PR を見る](https://github.com/microsoft/aspire/pull/18839)

3. **PR #18846: Aspire Team App: card actions, per-repo skill routing, streaming UX** リポジトリ横断の PR レビューキュー拡張機能に、カードごとのアクションボタンやリポジトリ単位のスキルルーティング、ストリーミング更新による体感速度改善を追加しています。EMU アカウント向けの既定リポジトリ設定や、ドロップダウンメニューの表示崩れ修正なども含まれています。

[PR を見る](https://github.com/microsoft/aspire/pull/18846)

## dotnet/runtime

.NET のランタイム、ライブラリ、コンパイラ基盤を含む中核リポジトリで、今回は6リポジトリ中もっとも件数が多い28件がマージされました。wasm 関連の JIT・コード生成の正確性向上を中心に、GC、ライブラリ（BCL）、ランタイム基盤、ビルド・テスト基盤の修正まで幅広く含まれています。

### wasm / JIT 関連

1. **PR #131401: [wasm] Assign frame offsets to dependently promoted parameter fields** wasm 版 JIT で、構造体パラメータの依存プロモートされたフィールドにフレームオフセットが正しく割り当てられず、`DateTime` の書式化などで誤った値を読み出す不具合がありました。対象条件に wasm を追加することで、フィールドが正しいオフセットで参照されるよう修正しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131401)

3. **PR #131360: [browser] Turn the >2GB memory test into an interop conformance test** wasm と JavaScript の境界では符号の扱いが異なるため、2GB を超えるメモリ領域でのポインタ受け渡しが静かに壊れる可能性がありました。単純な動作確認だったテストを、文字列・配列・`Span`・例外伝播など多岐にわたる相互運用の整合性を検証する本格的なテストに書き換えています。

[PR を見る](https://github.com/dotnet/runtime/pull/131360)

5. **PR #131374: [wasm] Report on-frame GC slots as pinned** wasm ではオペランドスタックに積まれた GC 参照のコピーを GC が更新できず、オブジェクトが再配置されるとスタックの値が古いアドレスを指したままになる不具合がありました。フレーム上に保持される GC スロットを pinned として報告することで、リフレクション経路などで発生していた断続的な `NullReferenceException` を解消しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131374)

7. **PR #130871: JIT: Remove boxing from value type instance calls** 値型のインスタンスメソッド呼び出しで不要なボックス化が発生し、余分なアロケーションが生じるケースがありました。`RefSafetyRulesAttribute` や `UnscopedRef` を考慮したうえで以前削除していた最適化を再導入し、安全な範囲でボックス化を回避するようにしています。

[PR を見る](https://github.com/dotnet/runtime/pull/130871)

9. **PR #131362: JIT: recompute exit weight in loop inversion** ループ反転処理でループ出口の重みを再計算せず既存のプロファイル情報をそのまま縮小適用していたため、もともと存在していたわずかな不整合が増幅されることがありました。出口の重みを反転処理の中で再計算することで、この増幅を防いでいます。

[PR を見る](https://github.com/dotnet/runtime/pull/131362)

11. **PR #131369: JIT: cache the wasm image base in a local** wasm 向けコード生成で、イメージベースアドレスを参照するたびに再配置情報を含むコードを生成しており、参照回数が多い関数でコードサイズが余分に膨らんでいました。3回以上参照される場合はローカル変数にキャッシュするようにし、R2R コードセクションのサイズを削減しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131369)

13. **PR #131429: Make wasm 'V' signature raising deterministic and add a regression test** wasm シグネチャの `'V'`（v128）型を復元する処理が、並列コンパイル時にたまたま最初にキャッシュされた v128 型に依存しており、コンパイル順序によって結果が変わり得る潜在的な不具合がありました。復元先を wasm ABI が定める固定の `Vector128<byte>` にすることで順序非依存にし、あわせて回帰テストを追加しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131429)

### ガベージコレクション（GC）

1. **PR #131324: Fix g_gc_lowest_address updating** GC が管理する仮想メモリ範囲を下方に拡張する際、境界条件の比較演算子の誤り（本来 `>` であるべき比較演算子が、意図とは異なる向きになっていた）により、まれに `g_gc_lowest_address` が 0 に設定されてしまい、null 参照を GC ヒープの一部と誤認する不具合がありました。比較条件を修正し、最小値が正しく OS ページサイズにクランプされるようにしています。

[PR を見る](https://github.com/dotnet/runtime/pull/131324)

3. **PR #130888: Fix GC.GetTotalMemory returning negative under regions GC** .NET 7 以降既定となっている regions GC 配下で、`GC.GetTotalMemory(false)` が符号なし整数のアンダーフローによりまれに負の値を返す不具合がありました。gen0 の全リージョンを走査してサイズを正しく集計するよう修正し、念のため結果が負にならないようクランプ処理も追加しています。

[PR を見る](https://github.com/dotnet/runtime/pull/130888)

### ライブラリ・BCL 関連の修正

1. **PR #130445: Fix reflection XmlSerializer choice identifier with XmlEnum aliases** リフレクションベースの `XmlSerializer` で、`[XmlChoiceIdentifier]` の解決を列挙体メンバー名と XML 要素名の一致だけに頼っていたため、`[XmlEnum]` によるエイリアスを使う列挙体が正しく往復変換できない不具合がありました。IL 生成版のリーダーと同様に要素の位置情報から選択値を解決するよう変更し、エイリアスにも対応しています。

[PR を見る](https://github.com/dotnet/runtime/pull/130445)

3. **PR #129493: support isSequence in reflection xml serializer** リフレクションベースの `XmlSerializer` は順序指定されたメンバー（`xsd:sequence`）を常に非順序として扱っており、同じ要素名を順序だけで区別するメンバーが誤って割り当てられる不具合がありました。IL 生成版と同じ順序管理のステートマシンを実装し、順序指定を正しく扱えるようにしています。

[PR を見る](https://github.com/dotnet/runtime/pull/129493)

5. **PR #130881: Add `contentEncoding: base64` to JSON schema for byte[], Memory<byte>, and ReadOnlyMemory<byte>** `System.Text.Json` はバイト配列を base64 文字列としてシリアライズしているにもかかわらず、`JsonSchemaExporter` が生成する JSON Schema にはその旨を示す `contentEncoding` が含まれていませんでした。`byte[]` や `Memory<byte>` 系の変換器に `contentEncoding: base64` を出力させ、仕様に沿ったスキーマにしています。

[PR を見る](https://github.com/dotnet/runtime/pull/130881)

7. **PR #131397: Support CIDR notation in NO_PROXY env variable** `NO_PROXY`/`no_proxy` 環境変数に CIDR 表記（例: 127.0.0.0/8）を指定しても、これまでは完全一致のエントリしか認識されず、静かに無視されていました。`HttpEnvironmentProxy` に IPv4/IPv6 の CIDR 解析を追加し、構築時に一度だけ解析することでリクエストごとの負荷も抑えています。

[PR を見る](https://github.com/dotnet/runtime/pull/131397)

9. **PR #130689: Avoid materializing `Task`/`ValueTask` in `StreamReader`/`StreamWriter`** `StreamReader`/`StreamWriter` は同時読み書きを検知する診断のために Task オブジェクトの生成を強制しており、ランタイム非同期呼び出し時に本来不要なアロケーションが発生していました。診断を try-finally ベースの実装に切り替えつつ、既存の async1 呼び出し元への影響を避けるため一部経路では Task ベースの挙動を維持しています。

[PR を見る](https://github.com/dotnet/runtime/pull/130689)

11. **PR #131417: Fix the net481 build break in AllocationHelper** 共有テストヘルパーが `Marshal.AllocHGlobal` から `NativeMemory` へ切り替わった際、`NativeMemory` が存在しない `net481` ターゲットのビルドが壊れていました。`net481` 向けにテスト専用のポリフィルを追加することで、モダンな .NET 向けの実装を変えずにビルドエラーを解消しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131417)

13. **PR #126093: FileConfigurationProvider: Handle and forward IO exceptions to OnLoadException** `FileConfigurationProvider` は、データ解析エラーと IO エラーとで `OnLoadException` コールバックの呼ばれ方や再読み込み時の挙動が一貫していませんでした。IO エラーもデータ解析エラーと同じ経路で `OnLoadException` に渡すよう統一し、`OnReload` は実際にデータが変化した場合のみ発火するよう調整しています。

[PR を見る](https://github.com/dotnet/runtime/pull/126093)

15. **PR #131424: Feature-detect KMAC key size limits** OpenSSL の既定プロバイダー上の KMAC には鍵長 4〜512 バイトという制限がある一方、Windows や SymCrypt-OpenSSL にはこの制限がなく、どのプロバイダーが有効かを事前に判定するのは難しい状況でした。プロバイダーの存在を推測する代わりに、長さ0の鍵をインポートできるかどうかを実際に試すことで鍵長制限を判定するようテストを変更しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131424)

17. **PR #131427: [release/11.0-preview7] FileConfigurationProvider: Handle and forward IO exceptions to OnLoadException** #126093 の IO 例外ハンドリング修正を `release/11.0-preview7` ブランチへバックポートしたものです。ブランチが分岐済みでコードコンプリート前という状況のもと、既存の6件のテスト追加を含めて同じ修正を適用しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131427)

### ランタイム基盤・診断

1. **PR #130436: [Diagnostics] Serialize concurrent reporters in InProc Crash Reporter** クラッシュレポート生成は最初のスレッドのみが担当する仕組みでしたが、通常の致命的シグナル経路以外（例外レコードプールの枯渇や `FATAL_ASSERT` など）から直接呼ばれた場合、レポート生成中に他スレッドがプロセスを終了させてしまい、レポートが途中で切れる可能性がありました。`createdump` と同様に所有権を1つの `InterlockedCompareExchange64` で確定させ、他スレッドはパイプ待機で直列化するようにしています。

[PR を見る](https://github.com/dotnet/runtime/pull/130436)

3. **PR #124721: Implement a workaround for ld-prime hitting an assert on large addends** ARM64 や x64 向けの相対アドレス再配置をエミュレートする際、加算値（addend）が符号付き20ビット整数に収まらないと `ld-prime` がアサートに失敗する問題がありました。セクション内に一定間隔でアンカーラベルを配置し、加算値を最も近いアンカーからの相対値に調整することで、常に範囲内に収まるようにしています。

[PR を見る](https://github.com/dotnet/runtime/pull/124721)

5. **PR #130693: Fix native EH in external shared libs on macOS** macOS では、`dotnet` や `apphost` などのホスト実行体がいくつかの標準 C++ 例外の型情報をローカルにコピーして公開しており、外部の共有ライブラリを読み込むとリンカがその型情報をホスト側のコピーに解決してしまい、C++ の `catch` で例外型が一致しない不具合がありました。必要なシンボル以外の公開を止めることで、この問題を解消しています。

[PR を見る](https://github.com/dotnet/runtime/pull/130693)

7. **PR #131359: Remove PEAssembly::UrlToPath and PEAssembly::FindLastPathSeparator** `PEAssembly` 内にあった呼び出し元のない静的ヘルパー `UrlToPath` と、単純なラッパーに過ぎなかった `FindLastPathSeparator` を整理しています。前者は削除し、後者は2箇所の呼び出し元にインライン化することでコードを簡素化しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131359)

9. **PR #131358: Fix config binder source gen for a sole read-only collection ctor param** 設定バインダーのソースジェネレーターは、読み取り専用コレクション型のコンストラクター引数のみを持つ型に対して、実際には生成されない `Initialize` メソッドの呼び出しを出力してしまい、ビルドが失敗する不具合がありました。コンストラクター引数を持つ型であれば他のバインド可能なメンバーの有無にかかわらず `Initialize` を生成するよう修正し、リフレクション版バインダーとの整合性を取っています。

[PR を見る](https://github.com/dotnet/runtime/pull/131358)

11. **PR #131008: Start DBI changes needed for non-surrogate loading** cDAC ベースの DBI 経由でライブデバッグを行えるようにするための対応です。DBI 側でランタイムのコントラクト記述子アドレスを解決して cDAC 側のエクスポートに渡すことで、ライブの `ICorDebug` セッションを cDAC 経由で処理できるようにしています。

[PR を見る](https://github.com/dotnet/runtime/pull/131008)

### ビルド・テスト基盤とリリース関連

1. **PR #131396: Update copilot instructions** Copilot コーディングエージェントがビルド失敗をきっかけに変更を諦めてしまった実体験を踏まえ、Copilot 向けの作業指示ドキュメントを更新しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131396)

3. **PR #131380: [wasm] Bump chrome for testing - linux: 150.0.7871.186, windows: 150.0.7871.187, mac: 150.0.7871.187** github-actions ボットによる自動更新で、wasm のテストで使用する Linux・Windows・macOS 向け Chrome for Testing のバージョンをそれぞれ最新のビルドへ引き上げています。

[PR を見る](https://github.com/dotnet/runtime/pull/131380)

5. **PR #131201: [release/10.0] Source code updates from dotnet/dotnet** dotnet-maestro ボットによる自動化されたコードフロー更新（バージョン同期）で、`dotnet/dotnet` VMR 側の最新ビルドに合わせてソースコードと Microsoft.CodeAnalysis・Arcade SDK などの依存関係を `release/10.0` ブランチに反映しています。

[PR を見る](https://github.com/dotnet/runtime/pull/131201)

7. **PR #126108: Migrate JitOptimizationSensitive and GCStressIncompatible to SkipOnCoreClr attributes** `RequiresProcessIsolation` を必要とする MSBuild プロパティ経由のテストスキップ指定を、プロセス分離を伴わずに実行時にスキップできる `SkipOnCoreClr` 属性ベースの指定へ大規模に移行しています。約310ファイルに及ぶ変更により、テスト実行のオーバーヘッドとなっていたプロセス分離を減らすことを狙っています。

[PR を見る](https://github.com/dotnet/runtime/pull/126108)

## microsoft/agent-framework

.NET と Python の両方でエージェント型アプリケーションを構築するための Microsoft Agent Framework のリポジトリです。今回は .NET 側の細かな修正と、Python 側の機能追加・バグ修正に分けて紹介します。

### .NET SDK 関連

1. **PR #7310: .NET: Create session for tool approval agent when none provided** ツール承認ミドルウェア（`UseToolApproval`）を自動承認ルール付きで使い、かつ明示的なセッションを渡さずにエージェントを呼び出すと、最初のツール呼び出し後にモデルプロバイダーから `input` パラメータ不足のエラーが返る不具合がありました。セッションが渡されない場合は内部で自動的にセッションを作成し、自動承認の再呼び出し全体で会話履歴を維持するよう修正しています。

[PR を見る](https://github.com/microsoft/agent-framework/pull/7310)

3. **PR #7343: .NET: Add Microsoft.Agents.AI.LocalCodeAct to release solution filter** `Microsoft.Agents.AI.LocalCodeAct` パッケージがリリース用のソリューションフィルターに含まれておらず、NuGet に一度も公開されていなかった不具合を修正しています。該当プロジェクトをフィルターに追加し、以後のリリースビルドに含まれるようにしています。

[PR を見る](https://github.com/microsoft/agent-framework/pull/7343)

5. **PR #7324: .NET: Preserve table state after declarative EditTable Add** 宣言的な `EditTable`／`EditTableV2` は、Add 操作のたびに items 変数を新しいレコードで置き換えてしまい、2回目以降の Add は、変数がテーブルでなくなったことで失敗する不具合がありました。変異後のテーブルを変数に保持し続けるよう修正し、連続した追加操作が期待どおり動作するようにしています。

[PR を見る](https://github.com/microsoft/agent-framework/pull/7324)

### Python SDK 関連

1. **PR #7292: Python: [Feature]: Support OpenAI instructions in Responses API** OpenAI の Responses API が提供する、その場限りの指示を渡せる `instructions` パラメータを、これまでは会話履歴に `system` メッセージとして永続的に注入してしまい、単発の指示という本来の意図が損なわれていました。`instructions` をそのまま API へ渡すよう変更し、継続ターンには影響しない一時的な指示として機能するようにしています。

[PR を見る](https://github.com/microsoft/agent-framework/pull/7292)

3. **PR #7291: Python: Reject Windows junctions in FileSystemAgentFileStore** `FileSystemAgentFileStore` はルート配下に限定したアクセスを想定しつつシンボリックリンクを拒否していましたが、判定に `Path.is_symlink()` しか使っていなかったため、Windows のディレクトリジャンクション（シンボリックリンクとして報告されないリパースポイント）を検知できず、設定したルートの外までファイル探索が及ぶ可能性がありました。`lstat()` やジャンクション判定を組み合わせた共通の判定処理を追加し、リパースポイントを一貫して除外するようにしています。

[PR を見る](https://github.com/microsoft/agent-framework/pull/7291)

5. **PR #7333: Python: fix(python): handle callable class middleware safely in _determine_middleware_type (#6697)** `__call__` を実装したクラスインスタンスをミドルウェアとして渡すと、内部で `__name__` 属性に直接アクセスしていたため未処理の `AttributeError` が発生し、本来通知されるべき `MiddlewareException` がマスクされてしまう不具合がありました。`getattr` で安全に名前を取得するよう変更し、意図したとおり分かりやすいエラーメッセージが表示されるようにしています。

[PR を見る](https://github.com/microsoft/agent-framework/pull/7333)

7. **PR #7309: Python: Add TodoProvider and AgentModeProvider samples** Agent Framework に組み込まれているコンテキストプロバイダー `TodoProvider` と `AgentModeProvider` について、.NET 側には既にあったサンプルの Python 版が存在していませんでした。複数ターンにわたるタスク管理を示すスクリプト形式のサンプルと、モード切り替えを対話的に試せるサンプルをそれぞれ追加し、README にも導線を追加しています。

[PR を見る](https://github.com/microsoft/agent-framework/pull/7309)

## m5stack/StackChan

M5Stack を用いたロボット「StackChan」関連のリポジトリです。

集計期間内にマージされた PR はありませんでした。

---

## まとめ

- **microsoft/fluentui-blazor**: 0件 — 集計期間中は目立った動きがなく静穏でした。
- **microsoft/fluentui**: 4件 — ヘッドレスコンポーネントの API 整備と Storybook・Web Components まわりの細かな修正が中心でした。
- **microsoft/aspire**: 8件 — ダッシュボードの UX・セキュリティ修正と、CLI・開発者ツール拡張機能の強化が並行して進みました。
- **dotnet/runtime**: 28件 — 6リポジトリ中最多で、wasm 向け JIT・コード生成の正確性向上を軸に、GC・BCL・ランタイム基盤の地道な修正が幅広く含まれていました。
- **microsoft/agent-framework**: 7件 — .NET と Python 双方でエージェントランタイムの信頼性を高める修正と、Python サンプルの拡充が行われました。
- **m5stack/StackChan**: 0件 — 集計期間中は目立った動きがなく静穏でした。
