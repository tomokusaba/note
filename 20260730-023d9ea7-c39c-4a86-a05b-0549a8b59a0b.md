# 【2026/07/29】マージ PR ダイジェスト fluentui-blazor / fluentui / aspire / runtime / agent-framework / StackChan

## はじめに

この記事は、2026年7月29日 00:00:00Z から 21:01:30Z まで（両端を含む）の集計期間中に、以下6つの OSS リポジトリでマージされたプルリクエストをまとめたものです。

- microsoft/fluentui-blazor
- microsoft/fluentui
- microsoft/aspire
- dotnet/runtime
- microsoft/agent-framework
- m5stack/StackChan

普段からこれらのリポジトリを追いかけている方に向けて、各 PR の意図と影響を短くまとめています。本文中の ⚠️ **破壊的変更** は、アップデート前に一次情報を確認したい PR の目印です。

---

## microsoft/fluentui-blazor

今回は 1 件の PR がマージされました。

### `Prep for release v4.14.4`

   バージョン番号、WhatsNewファイル、ホームページの更新と、アイコンをバージョン 1.1.334 へ更新する、v4.14.4リリース準備のための PR です。機能追加や破壊的変更を意図したものではなく、リリース手前の定型的な準備作業に位置づけられます。

## microsoft/fluentui

今回は 3 件の PR がマージされました。

### `fix(docsite): restore LLM documentation after yarn v4 migration`

   Yarn 4への移行によってビルド後の暗黙フックが動かなくなり、React v9・Charts・Headless各docsiteのllms.txtが生成されなくなっていた問題を修正しています。あわせてv8 docsiteのリソースビルド依存をNx上で明示化し、不完全なLLM向け成果物が公開されないようにしています。

### `Voice control A11y fix v9 charts`

   v9のChartsコンポーネントを対象にした、音声操作まわりのアクセシビリティ修正です。詳細な変更内容は PR 本文からは確認できませんでしたが、タイトルからはChartsのボイスコントロール対応を改善するアクセシビリティ修正であることが読み取れます。

### `RFC: Headless Components (@fluentui/react-headless-components)`

   ベースとなるstate hooksとrender関数から組み立てる、スタイル無しの`@fluentui/react-headless`パッケージを提案するRFCです。CSSとの結合点として安定した`data-*`属性を契約にする方針で、既存のBase State Hooks RFCの上位レイヤーに位置づけられています。実装そのものではなく設計提案のマージである点に留意してください。

## microsoft/aspire

今回は 1 件の PR がマージされました。

### `Stop AppHost debug session before Aspire parent`

   VS Code拡張のE2Eテストでグローバルなデバッグ停止処理がタイムアウトしていた問題への対応です。実際のAppHostデバッグセッションを、合成的なAspire親デバッグセッションより先に停止するよう順序を揃え、AppHost停止に失敗した場合でも親セッションは`finally`ブロックで確実に停止されるようにしています。

---

## dotnet/runtime

集計期間中に 26 件の PR がマージされたため、内容ごとにカテゴリー分けしています。

### JIT・コード生成

### `JIT: rewrite wasm local.set/local.get as local.tee`

   エミッタのピープホール最適化で、wasmの`local.set`/`local.get`のペアを`local.tee`へ書き換えることで、R2R版corelibのコードセクションを2.56%縮小しています。実行結果に影響しない、コードサイズ最適化の PR です。

- `JIT: include element info when comparing array address trees`

   `GenTree::Compare`が配列要素のサイズやメタデータを比較時に無視していたため、要素型やスケールが異なる2つの配列要素アドレスをtail mergeが誤って統合してしまう不具合を修正しています。ハッシュ計算にもこれらのフィールドを含めるよう修正しています。

- `[Wasm RyuJit] assert function regions are contiguous after control flow`

   wasmコード生成では各リージョン（メインメソッドと各funclet）ごとに1つの関数本体を出力します。`fgWasmControlFlow`の後に各リージョンのブロックがブロック順序上で連続しているかを検証するデバッグアサートを追加し、制御フロー変換後の不整合を早期に検出できるようにしています。

- `[wasm] Fix JIT helper calls modeled with the wrong return arity`

   レジスタマシンでは問題にならない「戻り値を使わないヘルパー呼び出しをTYP_VOIDとしてモデル化する」というJITの近道が、戻り値の個数が厳密に一致する必要があるwasmでは関数シグネチャ不一致のトラップを引き起こしていた問題です。クラス初期化ヘルパーで先行して修正された同種の問題の、残り 3 件を修正しています。

- `Use an explicit allow list for SIMD types in getTypeLayout`

   `getTypeLayout`におけるSIMD型の判定を、これまでの「deny list方式（decimal浮動小数点型だけを除外）」から、`System.Numerics`と`System.Runtime.Intrinsics`のSIMD型を明示的に列挙するallow list方式に変更しています。JITのネイティブ側と管理コード側（crossgen2/ILCompiler）の両方に新しい判定ヘルパーが追加されています。

### 診断・デバッグ基盤

- `Remove length check on debug log content`

   診断機能まわりのデバッグログ出力における、内容の長さチェック処理を取り除く修正です。不要な長さ判定に依存せず、ログ内容をそのまま扱う経路に整理しています。

- `Fix crash dump exit code comparison`

   クラッシュダンプ生成時の終了コード比較ロジックの不具合を修正する PR です。関連Issueへの紐づけがあり、クラッシュ検知の正確性に関わる修正と位置づけられます。

- `[cDAC] Always assign IStringHolder in DacDbi GetModulePath for pathless modules`

   ディスク上にパスを持たないモジュール（動的生成メソッドやDLRなど）に対して、cDACの`DacDbiImpl.GetModulePath`が`S_OK`かつ`pResult=FALSE`を返しつつ`IStringHolder`を未設定のままにしていたため、ネイティブDBI側のアサートに引っかかり`ICorDebugModule::GetName`が失敗していた問題を修正しています。デバッガー利用時の「bad OnModuleLoad」表示につながっていた不具合です。

- `[Diagnostics] Avoid resolving IL offsets for framed crash frames`

   P/Invoke遷移中のネイティブSIGSEGVで、命令ポインタがフォールトしたネイティブ関数内にあるにもかかわらず、クラッシュレポーターがそのフレームのIL offsetを解決しようとしていた問題を修正しています。過去の PR で対応済みの箇所とは別の経路で発生していたケースへの追加対応です。

- `Add universal DBI with private PAL`

   DACと共有PALを借用する従来の`mscordbi`とは別に、自前のPALを持つ`mscordbi_universal`を新設し、`Microsoft.DotNet.Cdac.Transport`に同梱しています。cDACベースのICorDebug経路向けで、`OpenVirtualProcessImpl2`と`CoreCLRCreateCordbObject3`の2エントリポイントのみをエクスポートし、既存の`mscordbi`には手を加えていません。

- `Enable in-proc crash reporting on MacOS + Linux`

    既存のクラッシュレポート設定を利用して、in-procクラッシュレポーターをLinuxとmacOSでも有効化する PR です。`DOTNET_DbgEnableMiniDump`やクラッシュレポート系の環境変数の組み合わせによって、createdumpのJSONレポート生成やin-procレポーターの動作が切り替わります。

- `Harden dbgutil Windows PE export walk against corrupted or incomplete module data`

    `ICorDebugDataTarget`から読み取ったデータを信頼できない入力として扱い、dbgutilの`TryGetSymbol`におけるWindows PEエクスポートテーブル走査を、破損・未マップなデータに対しても安全になるよう防御的に書き換えています。

- `[cDAC] Validate contract presence in interface returning functions`

    cDACが`IXCLRDataProcess`・`ISosDac*`・CorDBオブジェクトを返す前に、それらが依存する全コントラクトをターゲットが実際に公開しているかを事前検証するようにしています。未対応・未認識・非サポートのコントラクトごとに分類可能なHRESULTを返すようになり、診断ツール側での原因切り分けがしやすくなります。

### ビルド・CI・インフラ

- `Unblock stacked PR CI by broadening PR branch filters in AzDO pipelines`

   スタックした PR が中間ブランチをターゲットにする際、AzDOの`pr`トリガーが`main`やリリースパターンに限定されていたため検証が自動実行されなかった問題を修正しています。対象パイプラインのブランチフィルターを、リリース系を除く全ブランチ、または全ブランチを含む方針に統一しています。

- `[browser][mono][MT] Uncaught TypeError: He._emscripten_force_exit is not a function`

   ChromeでのWasmマルチスレッドテスト実行時に発生していた`_emscripten_force_exit is not a function`というUncaught TypeErrorへの対応です。Helixのテストログに基づく、wasm/monoのテスト実行環境まわりの修正です。

- `Fix CLR_Tools_Tests build break in WasmArgumentLayoutTests`

   `linux-x64 checked CLR_Tools_Tests`レグで`main`ブランチのビルドが失敗していた問題への対応です。`ReadyToRunCompilerContext`コンストラクターの引数不足によるコンパイルエラーを修正し、ビルドを復旧させています。

- `[ci-scan-feedback] Route platform-specific crypto-config failures to loop-in`

   `System.Security.Cryptography.*`のテストが特定ディストリビューション固有の暗号設定でのみ失敗するケースに対するゲートが、`ci-failure-fix`のagentic workflowに存在しなかったため、直近30日の修正 PR 却下率が40%に達していた問題への対応です。該当パターンを検知した場合はloop-in（コメント）へ回すよう手順書を更新しています。

- `Provision wasm build/test tools into a shared, version-keyed cache`

   emsdkやwasi-sdkなど外部取得しているWebAssemblyツールが、それぞれ独自の配置場所・バージョン管理方式・失効判定を持っていた状態を整理し、共有のバージョンキー付きキャッシュへ統一する PR です。ツールごとに異なっていた運用ルールをそろえることで、更新時の取りこぼしを防ぐ狙いがあります。

- `Update CMakePresets.json to 3.26 version`

    `CMakePresets.json`のバージョン指定を3.26へ更新する、ビルド設定まわりの小さな変更です。コミュニティコントリビューションとしてマージされています。

### パフォーマンス改善

- `Improve MemoryCache performance under high contention`

   あるファーストパーティのベンチマークで、win-arm64環境で最大6倍の性能向上が確認された、`MemoryCache`の高競合状態でのパフォーマンス改善です。BenchmarkDotNetによる計測結果が PR 内で共有されています。

- `Vectorize BMP string`

   ビッグエンディアンのUCS-2エンコーディングであるBMPEncodingに対して、以前の PR に続く形でベクトル化を導入しています。国コードのような短い入力ではわずかな性能低下（約0.5ns）が生じる一方、一般的な名前などの入力では明確な性能向上が見られるとされています。

### ライブラリ・ランタイム動作

- `Fix deadlock between EventListenersLock and ArrayPoolEventSource static ctor`

   .NETアプリ起動時に`dotnet-counters`を開始すると、`EventListenersLock`と`ArrayPoolEventSource`の型初期化ロックの間でロック順序が循環し、デッドロックが発生していた問題を修正しています。`FrameworkEventSource`で過去に行った修正と同じパターンで、`ArrayPoolEventSource`を事前初期化することでロック循環を防いでいます。

- `Make XsdDataContractExporter export consistent for ctor-less collections` ⚠️ **破壊的変更（スキーマ出力の挙動限定）**

   パラメーターなしコンストラクターを持たないコレクション型のスキーマ出力が、プロセスグローバルなDataContractキャッシュへ最初にどう登録されたかというキャッシュ順序に依存して、例外が出たり出なかったりしていた不整合を修正しています。PR 本文でも「破壊的変更として正当かつ低リスクである」ことが明示されており、変更が及ぶのはスキーマ出力（export）のみで、シリアライズ/デシリアライズ時のコンストラクター検証は従来どおり維持されるとされています。

- `Fix config binding source generator duplicating collection items bound through a constructor parameter`

   パラメーター付きコンストラクターを持つ型をバインドする際、コンストラクター経由で渡されたコレクションプロパティの要素が、Configuration Bindingのソースジェネレーターによって重複して追加されてしまう不具合を修正しています。

- `Remove stale ML-DSA draft-10 PKCS#8 test gating`

   SignedCmsのPKCSテストから、古い`SupportsDraft10Pkcs8`というテスト機能フラグを削除し、対象のML-DSAカバレッジを`MLDsa.IsSupported`で直接ゲートするよう変更しています。Windows固有のdraft-10対応という過去の回避策を、現行のプラットフォーム機能チェックに揃えるテスト専用の変更です。

### 内部リファクタリング

- `Reconcile ReleaseHolder, ComHolderPreemp, and ComHolderAnyMode`

   3種類のCOMインターフェース用RAIIホルダー（`ReleaseHolder`・`ComHolderPreemp`・`ComHolderAnyMode`）を、`LifetimeHolder<Traits>`パターンに統一するリファクタリングです。`ComHolderPreemp`は`ReleaseHolder`に統合されて削除され、`ComHolderAnyMode`は`ReleaseHolderAnyMode`へ改称されています。

---

## microsoft/agent-framework

集計期間中に 8 件の PR がマージされたため、内容ごとにカテゴリー分けしています。

### .NET関連

- `.NET: Fix and re-enable flaky InputWaiter timeout test`

   `net472`/`windows-latest`環境の`merge_group`実行で断続的に失敗し検疫扱いになっていた`InputWaiter`のタイムアウトテストを、修正のうえ再度有効化しています。テストが計測していたのがタイムアウト挙動ではなくスレッドプールのスケジューリング遅延だった点を指摘し、関連する同種テストも合わせて修正しています。

- `.NET: Bump .NET SDK from 10.0.301 to 10.0.302`

   `dotnet/global.json`で固定している.NET SDKのバージョンを、セキュリティアドバイザリへの対応として10.0.301から10.0.302へ引き上げています。バージョン固定のみの変更で、機能的な影響はないとされています。

- `.NET: Add source (ZIP) deploy oriented hosted agent samples`

   ソース（ZIP）デプロイがPython/.NETのhosted agentで既定の方式になったことを受け、Dockerfile作成やコンテナレジストリ管理を必要としないZIPデプロイ中心のサンプルへ.NET向けサンプルを再構成しています。`Hosted-ChatClientAgent`は単一の`azure.yaml`からソースデプロイし、コンテナ経路が必要な人向けに`Hosted-ChatClientAgent-Dockerfile`を別途用意しています。

### Python関連

- `[BREAKING] Python: Allow workflow checkpoint full replayability` ⚠️ **破壊的変更**

   タイトルに`[BREAKING]`と明記され、GitHub上のラベルにも`breaking change`が付与されている PR です。ワークフローのチェックポイントを完全に再生（リプレイ）可能にする変更で、開始エグゼキューターがスーパーステップ 1 内で実行されるため、各実行のスーパーステップ数が1つ増え、`iteration_count`はHIL（human-in-the-loop）ライフサイクル全体では一意でなくなります。既存の永続化済みチェックポイントの互換性は維持されますが、適用前に PR 本文でこれらの挙動変化を確認することをおすすめします。

- `Python: Improve function approval resume and replay`

   Pythonの関数承認（approval）フローの再開処理が、呼び出し元が所有するメッセージを直接変更してしまう、解決済みの結果が再開後のレスポンスから漏れる、`call_id`をグローバルに一意とみなして誤対応してしまうといった問題を抱えていたことへの改善です。承認済み・却下済みの結果が呼び出し元から見えなくなる、あるいは古い承認権限が再生されるといった不具合の解消を狙っています。

- `Python: Fix OpenAIChatCompletionClient passing raw JSON-Schema dict response_format through unwrapped`

   生のJSON-Schema辞書が`response_format`としてそのままChat Completions APIへ渡され、OpenAI側が400エラーを返していた問題を修正しています。Responses系の`OpenAIChatClient`が既に行っている自動ラップ処理と挙動を揃える形で、`_normalize_response_format_dict`を追加しています。

- `Python: extract keywords from non-English text for topic selection`

   キーワード抽出用の正規表現がASCII専用だったため、CJKやキリル文字などラテン文字以外で書かれたメッセージからキーワードが一切抽出されず、英語以外のユーザーはメモリのトピックファイルが自動的に読み込まれていなかった問題を修正しています。正規表現をUnicode対応にしつつ、英語のトークン化結果は変えない形で修正されています。

### ドキュメント関連

- `docs: ADR-0033 feature-usage bitmask in the User-Agent`

   Agent FrameworkがSDKの通信は識別できるものの、どの機能が組み合わせて使われているかは把握できていないという課題に対し、承認済みの一次パーティ向けリクエスト経路についてUser-Agentのテレメトリに載せる、透明かつオプトアウト可能な機能利用ビットマスクを定義するドキュメントのみの設計提案です。v1は128ビットから始まり、通常のパッケージ・機能追加では直ちにレジストリv2化が必要にならない設計とされています。

## m5stack/StackChan

集計期間内にマージされた PR はありませんでした。

---

## まとめ

- 集計期間は 2026-07-29T00:00:00Z から 2026-07-29T21:01:30Z まで（両端含む）です。
- microsoft/fluentui-blazor は 1 件で、v4.14.4リリース準備の PR でした。
- microsoft/fluentui は 3 件で、docsite復旧、v9 Chartsのアクセシビリティ修正、Headless ComponentsのRFCマージが含まれます。
- microsoft/aspire は 1 件で、VS Code拡張のデバッグ停止順序に関する修正でした。
- dotnet/runtime は 26 件と最も多く、JIT・コード生成、診断・デバッグ基盤、ビルド・CI・インフラ、パフォーマンス改善、ライブラリ・ランタイム動作、内部リファクタリングに分けて紹介しました。
- microsoft/agent-framework は 8 件で、.NET関連、Python関連、ドキュメント関連の 3 カテゴリーに分けて紹介しました。
- microsoft/agent-framework の #7374 は `[BREAKING]` と明記され、`breaking change` ラベルも付いた破壊的変更です。dotnet/runtime の #131181 も、PR 本文で「破壊的変更として正当かつ低リスク」と説明されている、スキーマ出力（export）限定の破壊的変更でした。アップデート予定の方はこの 2 件を優先して確認することをおすすめします。
- m5stack/StackChan は、集計期間内にマージされた PR はありませんでした。
