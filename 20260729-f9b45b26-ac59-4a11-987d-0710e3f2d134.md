# 2026-07-29 OSS PR サマリー — アクセシビリティ改善とランタイム内部強化の一日

## はじめに

この記事は、UTC `2026-07-28T00:00:00Z` から `2026-07-28T21:02:30Z` までにマージされた OSS の Pull Request をまとめたものです。JST では `2026-07-29T06:02:30+09:00` に収集内容を確認しています。OSS の開発動向を手早く把握したいエンジニア向けに、各リポジトリの変更点を要約しています。

対象は `mergedAt` を基準にした区間で、開始時刻・終了時刻を両端含む（inclusive）形でフィルタリングしています。この区間内にマージされた PR のみを対象にし、区間外（同日でも区間より後にマージされたもの）は含めていません。

対象リポジトリは microsoft/fluentui-blazor、microsoft/fluentui、microsoft/aspire、dotnet/runtime、microsoft/agent-framework、m5stack/StackChan の 6 つです。このうち microsoft/aspire と m5stack/StackChan は、この区間内にマージされた PR がありませんでした。

---

## microsoft/fluentui-blazor

この区間では 7 件の PR がマージされました。フォーカス・アクセシビリティ関連の修正が中心で、dev-v5 系のコンポーネント刷新や依存関係の更新も含まれています。

### フォーカス・アクセシビリティの修正

1. **[#5041] Fix anchored region focus across shadow DOM and inline popups** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5041?WT.mc_id=DT-MVP-5004827)
   `FluentAnchoredRegion` のフォーカス処理を、Shadow DOM を含む合成ツリー（composed tree）をたどるように修正しました。ポップアップがまだ DOM 上に残っている間に `Tab` を押すと、閉じたはずのポップアップへフォーカスが戻ってしまう問題を解消しています。

3. **[#5003] Fix FluentDataGrid column width popup focus on tab navigation** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5003?WT.mc_id=DT-MVP-5004827)
   `FluentDataGrid` の列幅変更ポップアップで、`Tab` / `Shift+Tab` によってフォーカスがポップアップ外に出た際にポップアップを閉じるよう修正しました。Shadow DOM をまたぐフォーカス可能要素の判定に 2 つの不具合があり、それぞれ修正しています。

5. **[#5051] [dev-v5] KeyCode - Enhance event handling with SafeKeyboardEvent** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5051?WT.mc_id=DT-MVP-5004827)
   `FluentKeyCode` のイベントハンドリングを `SafeKeyboardEvent` でラップしました。ブラウザーのオートフィルなどが発生させる不完全な `KeyboardEvent`（`ctrlKey` や `key` が `undefined` になるケース）でも、JSON シリアライズ時に `System.Text.Json` が例外を投げてコンポーネントがクラッシュしないようにしています。

### dev-v5 コンポーネント刷新

1. **[#5017] [dev-v5][Text] Repurpose `As` parameter** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5017?WT.mc_id=DT-MVP-5004827)
   dev-v5 系の `Text` コンポーネントで、サポート対象外だった `as` 属性を Web Component ラッパーから削除しました。あわせて `As` パラメーターを、レンダリング結果のセマンティックな HTML 出力を制御する目的に作り直しています。

3. **[#4961] [dev-v5][Overflow] refactor to wrap web component** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/4961?WT.mc_id=DT-MVP-5004827)
   `FluentOverflow` を Web Component 先行の実装（`<fluent-overflow>`）に刷新し、リサイズ時の遅延を減らすとともに、`AppBar` のオーバーフロー処理も同じモデルに揃えました。`MoreButtonTemplate` を `MoreTemplate` にリネームするなど API 面の変更もあり、移行ガイドが追加されています。

### 保守

1. **[#5048] Bump js-yaml from 4.2.0 to 4.3.0 in /src/Core.Assets** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5048?WT.mc_id=DT-MVP-5004827)
   Dependabot による依存関係更新です。`/src/Core.Assets` 配下の `js-yaml` を 4.2.0 から 4.3.0 に引き上げました。

3. **[#5050] Update devDependencies to latest versions** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5050?WT.mc_id=DT-MVP-5004827)
   devDependencies を最新バージョンへ更新しました。`keyv` の脆弱性（名前空間分離の不備）への対応も含まれており、破壊的変更はないとされています。

---

## microsoft/fluentui

この区間では 3 件の PR がマージされました。チャートのアクセシビリティ改善と、`react-headless-components-preview` パッケージ関連の変更が含まれます。

1. **[#36426] fix(charts): improve legends accessibility and update snapshots** [PR を見る](https://github.com/microsoft/fluentui/pull/36426?WT.mc_id=DT-MVP-5004827)
   チャートの凡例（`Legends`）と `OverflowMenu` のアクセシビリティを改善しました。あわせて、変更の影響を受けるチャートコンポーネントのスナップショットも更新しています。

3. **[#36350] feat(react-headless-components-preview): add AvatarGroup component** [PR を見る](https://github.com/microsoft/fluentui/pull/36350?WT.mc_id=DT-MVP-5004827)
   ヘッドレスな `AvatarGroup` コンポーネントを追加しました。`AvatarGroupItem` と `AvatarGroupPopover` から成る複合コンポーネントで、ネイティブな HTML 要素と Popover API を使って実装されています。

5. **[#36454] fix(react-headless-components-preview): fix multiple anchors support on the same element** [PR を見る](https://github.com/microsoft/fluentui/pull/36454?WT.mc_id=DT-MVP-5004827)
   `usePositioning` フックをリファクタリングし、同一要素に複数のアンカーを設定した場合の対応を修正しました。`readAnchorNames` 関数をモジュールスコープへ移動し、不要な反復処理を避ける短絡評価も加えています。

---

## microsoft/aspire

この区間でマージされた PR はありませんでした。

---

## dotnet/runtime

この区間では 27 件の PR がマージされました。JIT・ランタイムのコード生成、cDAC 診断や Edit and Continue（EnC）まわりの内部実装、例外処理、BCL のライブラリ改善、インフラ・開発体験、サービシングとテスト安定化まで幅広い変更が含まれます。

### JIT・ランタイムのコード生成

1. **[#131471] JIT: don't merge assertions from orphaned pred blocks** [PR を見る](https://github.com/dotnet/runtime/pull/131471)
   RBO のジャンプスレッディングが `bbPreds` を持たないブロックを削除せずに残すケースで、`RangeCheck::MergeAssertion` が古いエッジのアサーションを無条件に読んでしまい誤ったコードが生成される問題を修正しました。到達不能ブロックのチェックを phi 引数のケースにも適用しています。

3. **[#131439] Do not allow generic inlining of ldftn requiring a fat function pointer** [PR を見る](https://github.com/dotnet/runtime/pull/131439)
   `ldftn` が隠れたジェネリックコンテキスト引数を必要とし、`fat function pointer`（コンテキスト情報を付加した関数ポインター）を要求するケースで、インライン化元の呼び出し元ではなくインライン化先（inlinee）のジェネリックパラメーターに基づくルックアップが誤って記録される不具合を修正しました。アリティが一致しない場合に `IndexOutOfRangeException` が発生する事象に対応しています。

5. **[#131345] Handle Decimal32/64/128 ABI packing in the VM and AOT type system** [PR を見る](https://github.com/dotnet/runtime/pull/131345)
   `System.Numerics.Decimal32/64/128` に、`Int128` / `UInt128` と同様の ABI 特別扱い（アライメントやバイト値渡しの相互運用）を追加しました。従来はこれらの型に対する特別扱いがなく、誤ったコード生成につながっていました。

7. **[#131219] [mono] Fix interface call in case of reabstraction** [PR を見る](https://github.com/dotnet/runtime/pull/131219)
   mono ランタイムで、インターフェースメソッドの再抽象化（reabstraction）が行われた場合に vtable スロットの計算が誤っていた問題を修正しました。IMT スロット経由のインターフェース呼び出しが正しいメソッドにディスパッチされるようにしています。

9. **[#131167] [Wasm Ryujit]: implement runtime-async codegen for R2R Wasm** [PR を見る](https://github.com/dotnet/runtime/pull/131167)
   Wasm 向け RyuJIT に、R2R Wasm 向けの runtime-async コード生成サポートを追加しました。継続（continuation）を Wasm のグローバル経由で返す仕組みや、非同期再開情報の読み取り専用データへの出力などが含まれます。

11. **[#128148] Arm64: Initialization of TYP_SIMD/TYP_MASK locals** [PR を見る](https://github.com/dotnet/runtime/pull/128148)
    Arm64 で `TYP_SIMD` / `TYP_MASK` ローカル変数の初期化処理を実装しました。ゼロ初期化はベクトルストアのループで行い、デバッグ用のポイズニングは `TYP_SIMD` にのみ適用し `TYP_MASK` には行わないようにしています。

13. **[#127520] Arm64 SVE: Support scalable constant vectors and masks** [PR を見る](https://github.com/dotnet/runtime/pull/127520)
    Arm64 SVE 向けに、サイズが可変（scalable）なベクター定数・マスク定数を `GenTreeVecCon` / `GenTreeMskCon` でサポートしました。定数は「繰り返し値」「開始値とステップを持つ数列」「先頭レーンのみ値を持ち残りゼロ」のいずれかとして表現され、`Sve.CreateTrueMask*()` や `Vector.Create()` などの API で有効になっています。

### 例外処理・デバッガー

1. **[#131467] [release/10.0] Fix EH - throw from catch in UnmanagedCallersOnly** [PR を見る](https://github.com/dotnet/runtime/pull/131467)
   `release/10.0` へのバックポートです。`UnmanagedCallersOnly` としてマークされたメソッドの catch 内から例外がスローされた際、Windows 上で無限ループとスタックオーバーフローに至る不具合を修正しています。.NET 9 で新しい例外処理がデフォルト有効化されて以降のリグレッションとされています。

3. **[#131446] Hide remaining managed EH helpers from the debugger stack** [PR を見る](https://github.com/dotnet/runtime/pull/131446)
   マネージド例外処理ヘルパーのうち、以前の修正で見落とされていた `AppendExceptionStackFrameViaClasslib` や `UpdateStackTrace`、`StackFrameIterator.Next` の各オーバーロードがあります。これらに `[StackTraceHidden]` と `[DebuggerHidden]` を付与し、デバッガーの呼び出しスタックに表示されないようにしました。

### ランタイム内部・診断（cDAC / EnC）

1. **[#131444] Fix missed case of COOP to PREEMPT transition** [PR を見る](https://github.com/dotnet/runtime/pull/131444)
   GC 実行モードを COOP（協調）から PREEMPT（プリエンプティブ）へ遷移させる処理で、見落とされていたケースを修正しました（Issue #131435 に対応）。

3. **[#131443] Fix EnC static field address cDAC API** [PR を見る](https://github.com/dotnet/runtime/pull/131443)
   cDAC の `GetEnCStaticFieldDataAddress` API を、`EnCAddedStaticField::GetFieldData` における値型・参照型の扱いに合わせて修正しました。EnC で追加された static フィールドのアドレス取得が正しく行われるようになります。

5. **[#131384] Implement CLRDataMethodDefinition IL extent APIs in cDAC** [PR を見る](https://github.com/dotnet/runtime/pull/131384)
   cDAC 側に `CLRDataMethodDefinition` の IL エクステント列挙 API（`StartEnumExtents` / `EnumExtent` / `EndEnumExtents`）と代表エントリアドレス取得 API を実装し、従来のフォールバック処理を置き換えました。tiny/fat IL ヘッダーなど、既存 DAC のセマンティクスは維持しています。

7. **[#130159] Use CodeVersioning for EnC** [PR を見る](https://github.com/dotnet/runtime/pull/130159)
   Edit and Continue（EnC）による編集を `CodeVersioning` の仕組み上で扱うようにしました。以降の各編集は 1 つの `ILCodeVersion`（対応する `NativeCodeVersion` を伴う）として管理され、`ILCodeVersionNode` に追加した `Source` フィールドで由来を区別します。EnC と ReJIT が併用できない点は従来通りです。

### ライブラリ（BCL）

1. **[#131452] Use Reflection.Emit accessors when dynamic code is supported** [PR を見る](https://github.com/dotnet/runtime/pull/131452)
   動的コード生成がサポートされる環境では、`Reflection.Emit` ベースのメンバーアクセサーを使うよう戻しました。`#130503` で行われたアクセサー選択方式の変更のみを差し戻したもので、リフレクション呼び出しの修正やテストはそのまま残しています。ベンチマークでは、`#130503` の変更後にリフレクションベースのシリアライズが約 2.08 倍遅くなっていたことが確認されています。

3. **[#131425] Update SetCredentialsAttributesW to match the native C API declaration** [PR を見る](https://github.com/dotnet/runtime/pull/131425)
   `SetCredentialsAttributesW` の P/Invoke 宣言を、Windows 上で 4 バイトである C の `unsigned long` に合わせて `long` から `uint` に変更しました。

5. **[#131413] [browser] Remove AssetEntry from dotnet.d.ts public API** [PR を見る](https://github.com/dotnet/runtime/pull/131413)
   `dotnet.d.ts` の公開 API から、他のどの公開型からも参照されていなかった `AssetEntry` を削除しました。内部利用のための型定義自体は残されています。

7. **[#131387] Make Uri non-ASCII hex unescaping a bit faster** [PR を見る](https://github.com/dotnet/runtime/pull/131387)
   `Uri` の非 ASCII 文字に対する 16 進アンエスケープ処理を、境界チェックの削減とテーブルルックアップの活用によって高速化しました。

9. **[#131229] Reuse per-thread MemoryStream/Utf8JsonWriter in EventSourceLogger.ToJson** [PR を見る](https://github.com/dotnet/runtime/pull/131229)
   `EventSourceLogger.ToJson` で毎回生成していた `MemoryStream` と `Utf8JsonWriter` を、スレッドごとに再利用する形に変更し、ログイベントごとのアロケーションを削減しました。

11. **[#130428] Fix xsi:list round-tripping with space-separated serialization** [PR を見る](https://github.com/dotnet/runtime/pull/130428)
    `[XmlText]` を付けた配列的なメンバーが、シリアライズ時に区切り文字なしで連結されてしまい往復変換（round-trip）できない問題を修正しました。XML の 4 種類の空白文字で区切るリスト形式でシリアライズ・デシリアライズするようにしています。

13. **[#129492] Implement support for derived IXmlSerializable types in XmlSerializer** [PR を見る](https://github.com/dotnet/runtime/pull/129492)
    `XmlSerializer` のリフレクションベース実装で、`IXmlSerializable` を実装する派生型の `xsi:type` によるデシリアライズをサポートしました。従来は `NotImplementedException` になるか正しく処理できませんでした。

### インフラ・開発体験

1. **[#131462] Update winget install instructions in windows-requirements.md** [PR を見る](https://github.com/dotnet/runtime/pull/131462)
   `docs/workflow/requirements/windows-requirements.md` の `winget` インストール手順を、実機の Windows arm64 環境での検証を踏まえて更新しました。

3. **[#131441] Define `REPO_COMMIT_HASH` for singlefilehost** [PR を見る](https://github.com/dotnet/runtime/pull/131441)
   `singlefilehost` は coreclr の一部としてビルドされるためコミットハッシュが渡されず `dotnet --info` などで常に `static` と表示されていた問題を修正し、`SourceRevisionId` 経由で実際のコミットハッシュが反映されるようにしました。

5. **[#131420] [wasm] Don't import the Mono wasm props on the CoreCLR path** [PR を見る](https://github.com/dotnet/runtime/pull/131420)
   実験的な CoreCLR wasm SDK でブラウザー wasm アプリを発行すると、`_WasmBuildAppCore` ターゲットが存在せず `MSB4057` でビルドが失敗する問題を修正しました。`BrowserWasmApp.props` の import を Mono 向けに限定し、CoreCLR 側は独立したターゲットセットのままにしています。

7. **[#131312] Improve CI scanner duplicate KBE detection** [PR を見る](https://github.com/dotnet/runtime/pull/131312)
   CI の失敗自動スキャナーが、既知の失敗を重複起票してしまう問題に対応しました。テストメソッド名を完全な形でまず検索するようにし、存在しない AzDO 添付ファイル名への依存を GitHub Check の参照に置き換えています。

### サービシング・テスト安定化

1. **[#131214] [release/8.0-staging] Reduce EncryptedXml encoded DTD test workload** [PR を見る](https://github.com/dotnet/runtime/pull/131214)
   `release/8.0-staging` へのバックポートです。`EncryptedXml` の DTD エンコードに関するテスト負荷を削減しています。

3. **[#131211] [release/9.0-staging] Reduce EncryptedXml encoded DTD test workload** [PR を見る](https://github.com/dotnet/runtime/pull/131211)
   `release/9.0-staging` へのバックポートです。上記と同じく `EncryptedXml` の DTD エンコードテスト負荷を削減する変更です。

5. **[#130622] Preserve IL for CoreCLR interpreter fallback on Apple mobile** [PR を見る](https://github.com/dotnet/runtime/pull/130622)
   Apple モバイル向けの合成 ReadyToRun パブリッシュで、CoreCLR インタープリターのフォールバックに必要な IL がストリップされてしまう問題に対応しました。特定命令セットのフィックスアップを持つメソッドの IL を保持し、一時的なプロジェクト単位のストリップ除外設定を削除しています。

---

## microsoft/agent-framework

この区間では 5 件の PR がマージされました。.NET 側の修正と Python 側の修正に分かれています。

### .NET

1. **[#7365] .NET: Forward A2A MessageSendParams.Configuration in the A2A adapter** [PR を見る](https://github.com/microsoft/agent-framework/pull/7365?WT.mc_id=DT-MVP-5004827)
   A2A ホスティング層が `RequestContext.Configuration`（`SendMessageConfiguration`）を読んでいなかったため、呼び出し元の `acceptedOutputModes` や `historyLength` などをエージェント側から参照できるようにしました。`A2AAgentHandler` が設定全体を `AdditionalProperties["a2a.configuration"]` に格納します。

3. **[#7361] .NET: Workflows: quarantine flaky InputWaiter timeout test (#7360)** [PR を見る](https://github.com/microsoft/agent-framework/pull/7361?WT.mc_id=DT-MVP-5004827)
   `merge_group` のキューをブロックしていた不安定なテスト `InputWaiterTests.InputWaiter_WaitForInputAsync_CompletesWhenTimeoutExpiresAsync` を隔離（quarantine）しました。`net472` / `windows-latest` の実行環境でスレッドプールの逼迫によりタイムアウト判定のタイミングが崩れることが原因とされています。

5. **[#7353] .NET:  Preserve table state across declarative EditTable operations** [PR を見る](https://github.com/microsoft/agent-framework/pull/7353?WT.mc_id=DT-MVP-5004827)
   宣言的な `EditTable` 操作（`Remove`、`Clear`、`TakeFirst`、`TakeLast`）の後も `itemsVariable` がテーブルとして維持されるようにし、チェックポイントされたテーブルが再開後も編集可能な状態を保つよう修正しました。

### Python

1. **[#7342] Python: isolate dependency-bound validation** [PR を見る](https://github.com/microsoft/agent-framework/pull/7342?WT.mc_id=DT-MVP-5004827)
   依存関係の境界検証で、パッケージ単位のプローブが関連しない他パッケージの拡張（extra）まで巻き込んでしまい、`agent-framework-ag-ui` の新しい FastAPI 候補が誤って非互換と判定される問題を修正しました。選択されたサーフェスのみを解決する共通ロジックを導入しています。

3. **[#7322] Python: Fix Gemini harness tool declarations** [PR を見る](https://github.com/microsoft/agent-framework/pull/7322?WT.mc_id=DT-MVP-5004827)
   Gemini Developer API でネイティブツール（Google 検索グラウンディングなど）と Agent Framework の関数ツールを混在させた際に、サーバー側のツール呼び出し報告が有効になるよう修正しました。関数ツールのスキーマも JSON Schema フィールド経由でそのまま渡されるようになっています。

---

## m5stack/StackChan

この区間でマージされた PR はありませんでした。

---

## まとめ

- 収集対象は UTC `2026-07-28T00:00:00Z` から `2026-07-28T21:02:30Z` まで（両端含む、`mergedAt` 基準）。JST `2026-07-29T06:02:30+09:00` に確認。
- **microsoft/fluentui-blazor**（7 件）: Shadow DOM をまたぐフォーカス処理の修正が目立ち、dev-v5 系のコンポーネント刷新（`FluentOverflow`）が進んでいます。依存関係の更新も 2 件含まれます。
- **microsoft/fluentui**（3 件）: チャート凡例のアクセシビリティ改善に加え、`react-headless-components-preview` の Popover/AvatarGroup 関連の変更。
- **microsoft/aspire**（0 件）: この区間でマージされた PR なし。
- **dotnet/runtime**（27 件）: JIT・ランタイムのコード生成修正が中心で、cDAC 診断や Edit and Continue（EnC）の内部実装、例外処理、BCL、インフラ、サービシングまで幅広い。
- **microsoft/agent-framework**（5 件）: .NET 側は A2A・Workflows・EditTable、Python 側は依存関係検証と Gemini ツール連携の修正。
- **m5stack/StackChan**（0 件）: この区間でマージされた PR なし。
- 明示的に「breaking change」とラベル付けされた PR はなかったが、`microsoft/fluentui-blazor` の #4961 では `MoreButtonTemplate` から `MoreTemplate` への API 名称変更と移行ガイドの追加があり、利用側での追随が必要になる可能性があります。
