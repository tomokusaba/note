# 2026-08-16 OSS PR サマリー — 主要リポジトリ動向

## はじめに

2026-08-16 06:02 JST 時点で確認した、主要 OSS リポジトリのマージ済み PR をまとめます。対象期間は 2026-08-15 00:00:00 UTC から 2026-08-15 21:02:12 UTC です。

対象は、`microsoft/fluentui-blazor`、`microsoft/fluentui`、`microsoft/aspire`、`dotnet/runtime`、`microsoft/agent-framework`、`m5stack/StackChan` の 6 リポジトリです。期間中に取り込まれた変更の内容と、利用者に関わる点を短く追います。

## microsoft/fluentui-blazor（1件）

### テキスト入力の即時処理とフォーカス管理

1. **#5150 — [dev-v5] Implement immediate input handling with improved focus management** [PR を見る](https://github.com/microsoft/fluentui-blazor/pull/5150)  
   テキスト入力の Immediate 処理をデバウンス化し、サーバー側ではシーケンス番号と `SemaphoreSlim` によって遅延時のイベント順序逆転を防ぐ変更です。フォーカス中のブラウザー値を固定して再レンダリングによる入力の崩れを抑え、テストダブルも追加することで、即時入力を使う利用者の操作安定性を高めます。

## microsoft/fluentui（0件）

集計期間内にマージされた PR はありませんでした。

## microsoft/aspire（8件）

### パッケージ取得と開発ツール

1. **#19417 — [release/13.5] Fix npm registry hard-coded to internal dnceng feed (#19370)** [PR を見る](https://github.com/microsoft/aspire/pull/19417)  
   `aspire agent init` が Playwright CLI を導入する際に、匿名アクセスで 401 となる内部 Azure Artifacts フィードを参照して失敗する問題を、公開 npm registry URL の利用へ切り替えて修正する release/13.5 向けバックポートです。回帰テストも加えられており、該当コマンドを使う利用者はパッケージ取得の失敗を避けやすくなります。

3. **#19373 — Update gRPC packages to 2.83.0** [PR を見る](https://github.com/microsoft/aspire/pull/19373)  
   `Grpc.AspNetCore`、`Grpc.Net.ClientFactory`、`Grpc.Tools` を 2.83.0 に更新し、古いプロジェクト単位のバージョン上書きと重複した直接参照を整理する変更です。Linux ARM64 での `protoc` クラッシュ修正を含む更新にそろえることで、該当環境で gRPC を扱う利用者にとっての安定性向上につながります。

### Kubernetes とデプロイ検証

1. **#19412 — Fix deployment E2E test matrix enumeration** [PR を見る](https://github.com/microsoft/aspire/pull/19412)  
   `AksPersistentVolumeDeploymentTests` に意図せず付いた Partition trait により、E2E テストマトリクスが 2 バケットへ縮退していた問題を修正します。Aspire のデプロイ検証を担う開発者・メンテナーは、クラスごとに復元された 42 ジョブで本来想定された組み合わせを確認できるようになります。

3. **#19401 — [release/13.5] Set a default fsGroup for Kubernetes persistent volumes** [PR を見る](https://github.com/microsoft/aspire/pull/19401)  
   実験的な `WithPersistentVolume(...)` で non-root ワークロードが PVC に書き込めない問題に対し、既定の `fsGroup: 2000` と `OnRootMismatch` を導入する release/13.5 向けバックポートです。Kubernetes の永続ボリュームを利用する non-root ワークロードでは、書き込み権限に起因する失敗を減らす意図があります。

5. **#19399 — [release/13.5] Report actionable errors when DotnetProjectResource is published** [PR を見る](https://github.com/microsoft/aspire/pull/19399)  
   `DotnetProjectResource` を公開した際のエラーを、次に取るべき対応がより分かりやすい内容に改善する release/13.5 向けバックポートです。公開時に問題へ遭遇した利用者が、状況を把握して対処を進めやすくなります。

### 診断メッセージ

ASPIRE010 の改善は、本体となる PR #19375 と、その release/13.5 向けバックポートである PR #19415 を併せて掲載します。

1. **#19415 — [release/13.5] Surface ASPIRE010 help link inline in warning text** [PR を見る](https://github.com/microsoft/aspire/pull/19415)  
   ASPIRE010 の警告テキスト内にヘルプリンクを直接表示する変更を、release/13.5 にバックポートします。`AspireUseCliBundle=false` の設定で警告を受け取った利用者は、コンソールから診断情報へたどり着きやすくなります。

3. **#19375 — Surface ASPIRE010 help link inline in warning text** [PR を見る](https://github.com/microsoft/aspire/pull/19375)  
   `AspireUseCliBundle=false` の AppHost 設定時に出る MSBuild 警告 ASPIRE010 について、コンソールに表示されなかった HelpLink を警告文内へ直接追加します。警告の直後に `https://aka.ms/aspire/diagnostics/aspire010` を確認できるため、設定を調べる導線が明確になります。

### ターミナルホストの終了処理

1. **#19344 — Handle SIGTERM in terminal host and GC orphaned sockets (#19302)** [PR を見る](https://github.com/microsoft/aspire/pull/19344)  
   `WithTerminal()` の終了時や親 AppHost の異常終了時に Unix ドメインソケットが残る問題へ、SIGINT・SIGTERM を共通で処理する終了手順と親プロセス監視を追加します。所有者がいないソケットを掃除することで、ターミナルホストを使う利用者は終了後の残存リソースを減らせます。

## dotnet/runtime（5件）

### プラットフォーム互換性と WebAssembly

1. **#132345 — Restore Windows Arm64 support for CPUs without LSE** [PR を見る](https://github.com/dotnet/runtime/pull/132345)  
   Windows Arm64 の JIT と NativeAOT の最低要件を Armv8-A に戻し、LSE を任意の CPU 機能として扱う変更です。LSE 非対応の NXP i.MX 8 搭載 Windows 10 IoT でも起動可能にしつつ、R2R は従来の Armv8.2-A と RCPC 要件を維持し、条件を満たさない場合は JIT へフォールバックします。

3. **#132248 — [wasm] Encode struct alignment in thunk signatures** [PR を見る](https://github.com/dotnet/runtime/pull/132248)  
   WebAssembly で 16 バイトアラインメントの構造体を値渡しできるよう、thunk signature のエンコードと遷移処理を更新します。Crossgen と Wasm R2R のテストも追加され、該当する構造体を扱う WebAssembly 利用者の実行経路を検証できるようになります。

### 診断 API とテストの安定化

1. **#132302 — Add locking for cDAC APIs** [PR を見る](https://github.com/dotnet/runtime/pull/132302)  
   同じ Target を共有する cDAC API 呼び出しが同時に実行されないよう、排他制御を追加します。診断 API を並行利用する場面での安全性を高めることが目的です。

3. **#132342 — Skip GitHub_130545 on NativeAOT** [PR を見る](https://github.com/dotnet/runtime/pull/132342)  
   NativeAOT では反射メタデータの generic constraint の解決が `TypeLoadException` となる既知の問題があるため、該当する回帰テストだけを一時的にスキップします。CoreCLR と Mono のカバレッジは維持し、根本課題は継続して追跡するため、テスト実行時の既知障害を切り分けやすくします。

### WinRT interop の並行性能

1. **#132033 — Partition the ComWrappers RCW cache into per-processor buckets** [PR を見る](https://github.com/dotnet/runtime/pull/132033)  
   ComWrappers の RCW identity cache を単一ロックからプロセッサ数に応じたバケットへ分割し、finalizer とネイティブからマネージドへの遷移が競合しにくい構成へ変更します。同じバケットを確実に検証するテストも更新され、高並列時の WinRT interop の性能改善を意図しています。

## microsoft/agent-framework（0件）

集計期間内にマージされた PR はありませんでした。

## m5stack/StackChan（0件）

集計期間内にマージされた PR はありませんでした。

## まとめ

- `microsoft/fluentui-blazor` では、即時入力時のイベント順序とフォーカス中の表示を安定させる変更が入りました。
- `microsoft/aspire` では、npm registry の参照先と gRPC パッケージの更新により、開発ツールと Linux Arm64 環境の安定性に関する変更が入りました。
- `microsoft/aspire` では、E2E テストマトリクス、Kubernetes 永続ボリューム、`DotnetProjectResource` の公開時エラーを扱い、デプロイ検証と公開時の対処を支えます。
- `microsoft/aspire` では、ASPIRE010 の診断導線とターミナルホスト終了後のソケット掃除が改善されました。
- `dotnet/runtime` では、Windows Arm64 の互換性と WebAssembly の構造体受け渡しに加え、診断 API の排他、NativeAOT の既知障害テストの切り分け、WinRT interop の並行性能が扱われました。
- `microsoft/fluentui`、`microsoft/agent-framework`、`m5stack/StackChan` では、集計期間内のマージ済み PR はありませんでした。