# 2026年8月16日 OAuth アプリの認証設定がより安全で柔軟に

GitHub は OAuth アプリと GitHub App のリダイレクト URI、ならびに OAuth アプリのトークン運用を更新しました。OAuth アプリを運用しているなら、認証フローとコールバック URL の設定を見直す価値がある変更です。

## .NET Blog

今回取り上げる新規記事はありません。

## GitHub Changelog

### [OAuth アプリで複数のリダイレクト URI とトークン更新をサポート](https://github.blog/changelog/2026-08-14-multiple-redirect-uris-and-token-refresh-for-oauth-apps)

OAuth アプリでは、短期アクセストークンとリフレッシュトークンを利用できるようになりました。認可リクエストに `offline_access` スコープを含めると、8 時間で期限切れになるアクセストークンと、6 か月間有効なリフレッシュトークンを取得できます。新規アプリでは短期トークンが既定で有効です。

コールバック URL は、1 つの OAuth アプリに最大 10 個まで登録できます。開発・ステージング・本番など複数の環境やドメインを、アプリを分けずに扱えるようになります。

OAuth アプリと GitHub App では、リダイレクト URI ごとにワイルドカード一致を有効にすることもできます。ただし、ユーザーがコンテンツを置けるサブドメインやパスを含む構成では、意図しない URL へ認可コードが送られるおそれがあります。特に、リダイレクト URI が 1 つだけの既存アプリでは、この一致が従来から有効になっているため、不要であれば設定画面で無効化することが推奨されます。

これらの機能は GitHub Enterprise Server 3.23 にも含まれる予定です。OAuth アプリを保守している場合は、利用中の SDK のリフレッシュトークン対応を確認し、登録済みのリダイレクト URI とワイルドカード設定を棚卸ししておくと安心です。

## まとめ

- .NET Blog では、今回取り上げる新規記事はありません
- OAuth アプリで短期アクセストークンとリフレッシュトークンを利用できるようになりました
- コールバック URL は最大 10 個まで登録でき、環境ごとのアプリ分割を減らせます
- 既存アプリのワイルドカード一致は、意図しないリダイレクトを防ぐために見直しが必要です
