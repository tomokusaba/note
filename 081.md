# 2026-07-24 OSS PR サマリー — Fluent UI、.NET、Agent Framework の主要リポジトリ動向

## はじめに

2026年7月24日 07:26（JST）時点で確認した、6つの OSS リポジトリの直近24時間にマージされた PR をまとめます。対象は microsoft/fluentui-blazor、microsoft/fluentui、microsoft/aspire、dotnet/runtime、microsoft/agent-framework、m5stack/StackChan です。

集計期間は、UTC で **2026-07-22T22:26:19.2444643Z 以上、2026-07-23T22:26:19.2444643Z 未満**です。該当件数は fluentui-blazor が0件、fluentui が3件、aspire が3件、dotnet/runtime が34件、agent-framework が19件、StackChan が0件でした。件数の多い dotnet/runtime と agent-framework は、内容ごとに分類して紹介します。

---

## microsoft/fluentui-blazor

集計期間内にマージされた PR はありませんでした。

---

## microsoft/fluentui

1. **Headless コンポーネントのレンダー関数 import を整理**  
   Headless コンポーネントのレンダー関数を直接 re-export する形式に統一し、import 経路を整理しました。API 表記の変更を含みますが、PR では破壊的変更とは示されていません。  
   [PR を見る](https://github.com/microsoft/fluentui/pull/36439?WT.mc_id=DT-MVP-5004827)

3. **react-positioning の配置コールバックに可視性フラグを追加**  
   配置コールバックのイベント詳細で、最終配置に加え `escaped` と `referenceHidden` を取得できるようにしました。利用側は DOM や CSS の状態から可視性を推測せずに処理できます。  
   [PR を見る](https://github.com/microsoft/fluentui/pull/36432?WT.mc_id=DT-MVP-5004827)

5. **Dependabot の npm ロールアップを最大11件に制限**  
   npm セキュリティ更新の自動グルーピングをやめ、`/dependabot-rollup` で互換性のある minor／patch 更新を最大11件までまとめられるようにしました。major 更新、ダウングレード、非 SemVer、重複はロールアップ対象外です。  
   [PR を見る](https://github.com/microsoft/fluentui/pull/36438?WT.mc_id=DT-MVP-5004827)

---

## microsoft/aspire

1. **ダッシュボード停止メッセージの重複を修正**  
   `aspire dashboard run` を停止したときに、ダッシュボード固有と共通の停止メッセージが二重に出る問題を修正しました。キャンセル表示を共通基底へ集約し、停止時は一つのメッセージだけを出力します。  
   [PR を見る](https://github.com/microsoft/aspire/pull/18827?WT.mc_id=DT-MVP-5004827)

3. **AppHost ランチャーの不安定なキャンセルテストを修正**  
   macOS でプロセス観測の競合により失敗し得た AppHost ランチャーテストを、プロセス終了の待機と実行順序の制御で安定化しました。非同期操作にはタイムアウトも設定し、ハング時の診断を限定しています。  
   [PR を見る](https://github.com/microsoft/aspire/pull/18856?WT.mc_id=DT-MVP-5004827)

5. **プロジェクトテンプレートに .NET 11 サポートを追加**  
   9つのフレームワーク対応テンプレートで、オプトインの `net11.0`（.NET 11 Preview 6）を選べるようにしました。既定値は .NET 10 のままで、SDK マトリクスのテストや OpenAPI パッケージ置換も更新されています。  
   [PR を見る](https://github.com/microsoft/aspire/pull/18849?WT.mc_id=DT-MVP-5004827)

---

## dotnet/runtime

### WebAssembly、ReadyToRun、NativeAOT

1. **NativeAOT の dispatch cell 情報をコンパクト化**  
   インターフェイスメソッドとジェネリック仮想メソッドで使う dispatch cell 情報の符号化を Native Layout ベースへ置き換えました。NativeAOT のメタデータ表現を整理する変更です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130824?WT.mc_id=DT-MVP-5004827)

3. **R2R で SZ 配列のメソッドをコンパイル可能に**  
   配列が実装するジェネリックインターフェイスのメソッドを `SZArrayHelper` と対応付け、ReadyToRun で条件付きにコンパイルできるようにしました。不要なメソッド増加を抑えながら、一部ベンチマークの性能問題を解消します。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130413?WT.mc_id=DT-MVP-5004827)

5. **Wasm のジェネリック Vector128 intrinsic を JIT が認識**  
   WebAssembly 向け JIT で、ジェネリックな `Vector128` intrinsic を認識できるようにしました。PR の説明が簡潔なため、ここでは対象機能の認識改善として紹介します。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131227?WT.mc_id=DT-MVP-5004827)

7. **Wasm SIMD パラメーターからのスカラー抽出を修正**  
   SIMD レジスタ引数のフィールドをスカラーとして取り出す際に、`v128` とスカラーの型不一致となる Wasm コード生成バグを修正しました。Wasm では適切な lane 抽出命令を使うようにしています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131237?WT.mc_id=DT-MVP-5004827)

9. **Wasm SIMD の Base64 と LeadingZeroCount の不足を補完**  
   Base64／Base64Url のベクター化に PackedSimd の分岐を加え、Wasm でスカラーへ落ちていた処理を改善しました。`byte`／`ushort` の `LeadingZeroCount` にも、ベクター命令がない環境向けのソフトウェアフォールバックを追加しています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131239?WT.mc_id=DT-MVP-5004827)

11. **Wasm の Vector128 引数用 thunk キーを修正**  
   interp と R2R 間で `Vector128<T>` を渡す際、VM 側が WebAssembly の `v128` と異なるキーを組み立てていた問題を修正しました。コンパイル側と実行時側のキーを一致させ、thunk が見つからない失敗を防ぎます。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131246?WT.mc_id=DT-MVP-5004827)

13. **Wasm AOT の Nullable<T> Box／Unbox ラッパーを生成**  
   Wasm AOT で `Nullable<T>` の Box／Unbox に必要なラッパーが生成されず、関数シグネチャ不一致で停止する問題を修正しました。ブラウザー向け `System.Text.Json` の nullable 数値に関するスキップ済みテストも復活しています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131049?WT.mc_id=DT-MVP-5004827)

15. **Wasm の R2R スタックウォークを修正**  
   インライン P/Invoke のセンチネルアドレスと composite R2R イメージの RVA ベースを正しく扱うようにしました。スタックウォークの無限反復や誤ったアドレス解決を防ぎます。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131243?WT.mc_id=DT-MVP-5004827)

### JIT、ランタイム、数値処理

1. **Mono LLVM の MinNumber／MaxNumber で符号付きゼロを保持**  
   Mono LLVM AOT で `MinNumber`／`MaxNumber` が `-0` の符号を失う問題を修正しました。IEEE 754-2019 の意味論に合う LLVM intrinsic へ切り替えています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131172?WT.mc_id=DT-MVP-5004827)

3. **異なるバイト長の指数・仮数比較を修正**  
   カスタム `IFloatingPointIeee754<T>` 実装で、指数や仮数の符号付き表現が異なる長さの場合に比較結果が誤る問題を修正しました。既存の組み込み浮動小数点型には影響しない、ジェネリック経路の修正です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131205?WT.mc_id=DT-MVP-5004827)

5. **GC stress テストのポインター固定を修正**  
   非 GC 型への `pinned` 指定ではローカルが固定されず、GC stress で失敗していたテストを修正しました。固定された byref を用いる形に変更しています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131171?WT.mc_id=DT-MVP-5004827)

7. **RhpNewArrayFast の LoongArch64 実装を修正**  
   LoongArch64 で `MaxArrayElementSize` が引き金となる SIGBUS を修正しました。あわせて DASM の GC 情報出力も追加されています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130958?WT.mc_id=DT-MVP-5004827)

9. **AddressSanitizer が検出した delete の問題を修正**  
   基底ポインター経由の削除と、配列／非配列の `delete` 呼び出し不一致を修正しました。AddressSanitizer により検出されたメモリー管理上の問題への対応です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130932?WT.mc_id=DT-MVP-5004827)

11. **JIT の古い副作用フラグを除去**  
   残存していた `GTF_ORDER_SIDEEFF` フラグを除去し、条件分岐の変換や bool 最適化を可能にしました。副作用を持たないノードの最適化余地を広げる変更です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130007?WT.mc_id=DT-MVP-5004827)

13. **PerfMap 呼び出しを preemptive モードへ集約**  
   Windows でも利用可能な PerfMap API の契約を整え、必要な呼び出しを preemptive モードで行うようにしました。GC をトリガーしない経路を含め、安全な実行コンテキストへ整理しています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/129019?WT.mc_id=DT-MVP-5004827)

15. **UnmanagedCallersOnly の例外処理ループを修正**  
   Windows の `UnmanagedCallersOnly` で catch 内から例外を送出した場合、同じ catch を繰り返してスタックオーバーフローに至る問題を修正しました。例外アンワインドの経路を正しく認識します。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131163?WT.mc_id=DT-MVP-5004827)

### ネットワーク、セキュリティ、プラットフォーム

1. **Unix のチャネルバインディングをサポート**  
   Unix の `NegotiateAuthentication` サーバー側で、GSSAPI へチャネルバインディングトークンを渡せるようにしました。バインディング不一致は GSSAPI の結果として検出し、既存のクライアント側と整合させています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130758?WT.mc_id=DT-MVP-5004827)

3. **低レベル TLS ステートマシン API を追加**  
   呼び出し側がバッファー管理と I/O スケジューリングを制御できる、ノンブロッキング TLS API の `TlsContext`／`TlsSession` を追加しました。Linux、macOS、Windows、FreeBSD を対象にした新しい API 面です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130366?WT.mc_id=DT-MVP-5004827)

5. **kevent 実装で EV_RECEIPT の使用を停止**  
   `kevent` の変更リスト処理で `EV_RECEIPT` を使わないようにし、FreeBSD／OpenBSD 向けの回避コードを取り除きました。イベント登録エラーの扱いを実際の利用方法に合わせています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130372?WT.mc_id=DT-MVP-5004827)

7. **linux-bionic で CoreCLR と singlefilehost を有効化**  
   サポート対象の linux-bionic arm64／x64 で CoreCLR を有効にし、AppHost パックに singlefilehost を含めました。Termux／Android 向け自己完結型シングルファイルバンドルが機能するようになります。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131115?WT.mc_id=DT-MVP-5004827)

9. **未対応環境の Extended Protection で PNSE を送出**  
   Extended Protection が使えない環境で `PlatformNotSupportedException` を送出する変更を .NET 11 Preview 7 ブランチへバックポートしました。`NegotiateStream`／`NegotiateAuthentication` の文書化された挙動に合わせます。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131191?WT.mc_id=DT-MVP-5004827)

11. **Windows の AIA 上限の挙動差に対応**  
   `AiaCompletionHasLimits` が Windows の実装差を扱えるようにし、関連する失敗を避けました。AIA 取得のキャッシュ挙動を踏まえたテスト安定化でもあります。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131242?WT.mc_id=DT-MVP-5004827)

### 診断、ツール、テスト

1. **ILLink の async partial メソッドのクラッシュ再発テストを追加**  
   キャプチャーするラムダを含むジェネリック `async partial` メソッドで、ILLink が `KeyNotFoundException` を起こす事例の回帰テストを追加しました。Roslyn が状態マシン属性を出力しないケースを再現します。  
   [PR を見る](https://github.com/dotnet/runtime/pull/126733?WT.mc_id=DT-MVP-5004827)

3. **ISOSDacInterface18 で GCInfo を検査可能に**  
   SOS の `!GCInfo` 向けに、GC 情報のヘッダー、割り込み可能範囲、安全点、スロット寿命を cDAC 経由で取得する API を追加しました。ARM の Thumb ビットを含む相対オフセット計算も修正しています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/129761?WT.mc_id=DT-MVP-5004827)

5. **非 Exception オブジェクトを捕捉する runtime async テストを追加**  
   IL でのみ送出できる非 `Exception` オブジェクトを、runtime async と通常の async がどう扱うかを検証するテストを追加しました。一部の既知の相違は Active Issue として隔離されています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130506?WT.mc_id=DT-MVP-5004827)

7. **Windows で clr.aot を clang コンパイルした際の警告を修正**  
   Windows で `clr.aot` を clang でコンパイルしたときに検出される、主に符号あり／なし比較の問題を修正しました。警告をエラーとして扱うビルド設定との整合を取っています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131183?WT.mc_id=DT-MVP-5004827)

9. **fprintf の書式文字列を修正**  
   clang が検出した `fprintf` の不正な書式文字列を修正しました。PR 説明は簡潔ですが、コンパイラー診断に対応する修正です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131186?WT.mc_id=DT-MVP-5004827)

11. **ILASM の FullPdbThrower テストを無効化**  
   ILASM が portable PDB のみをサポートする前提に合わせ、round-trip テストの `FullPdbThrower` を無効化しました。サポート対象外の PDB 形式による失敗を避けます。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131238?WT.mc_id=DT-MVP-5004827)

13. **ILLink のネストした例外ハンドラー到達性を修正**  
   ネストした例外ハンドラーの到達性解析を繰り返し評価し、必要なクリーンアップコードが削除されないようにしました。回帰テストを加え、ハンドラーの探索順序に依存する問題を防いでいます。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131236?WT.mc_id=DT-MVP-5004827)

### ライブラリ、ビルド、リリース

1. **不定長 CBOR 文字列の非 ASCII データ処理を修正**  
   不定長文字列に含まれる非 ASCII データの処理を修正しました。PR 説明が空のため、タイトルで明示された対象に限定して紹介します。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131235?WT.mc_id=DT-MVP-5004827)

3. **RID グラフ生成を System.Text.Json へ移行**  
   RID グラフの生成で使用していた Newtonsoft.Json を System.Text.Json に置き換えました。ビルド時の JSON 処理を標準ライブラリへ寄せる変更です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131212?WT.mc_id=DT-MVP-5004827)

5. **破損した ZIP central directory の検証を追加**  
   ZIP ファイルの central directory が破損している場合を検出するチェックを追加しました。破損データへの処理を強化する修正です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131257?WT.mc_id=DT-MVP-5004827)

7. **BootJsonData 生成の更新を Preview 7 へバックポート**  
   `runtimeconfig.dev.json` を考慮する BootJsonData 生成の変更を、.NET 11 Preview 7 ブランチへバックポートしました。ブラウザー向けビルド出力の整合を取る変更です。  
   [PR を見る](https://github.com/dotnet/runtime/pull/131224?WT.mc_id=DT-MVP-5004827)

9. **ARM64 FP レジスタのデバッグ情報を追加**  
   ARM64 の V0〜V31 レジスタと、一部の FP／HFA／HVA 戻り値をデバッガーで検査できるようにしました。**破壊的変更**として、ReadyToRun フォーマットの major／minor バージョンを 26 へ引き上げています。  
   [PR を見る](https://github.com/dotnet/runtime/pull/130980?WT.mc_id=DT-MVP-5004827)

---

## microsoft/agent-framework

### Python の A2A、ワークフロー、ツール実行

1. **段階的な A2A ホスティングアダプターを追加**  
   `AgentA2AAdapter` と `WorkflowA2AAdapter` を追加し、型付きワークフロー変換、ドキュメント、テスト、サンプルを整備しました。ネイティブ A2A サーバーや実行処理の所有権は、引き続きアプリケーション側に残ります。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7258?WT.mc_id=DT-MVP-5004827)

3. **A2A サンプルの古い agent.json 参照を修正**  
   A2A サンプルの `/.well-known/agent.json` を、正しい `/.well-known/agent-card.json` へ修正しました。ディスカバリー要求と起動時に表示される URL が実際のエンドポイントと一致します。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7281?WT.mc_id=DT-MVP-5004827)

5. **AG-UI ワークフローキャッシュのスコープを整合**  
   解決済みのスナップショットスコープをインメモリーのワークフローキャッシュへ反映しました。設定したスコープごとに実行中のワークフローインスタンスを分離し、既定動作は維持します。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7277?WT.mc_id=DT-MVP-5004827)

7. **reasoning と対になるツール呼び出しの stateless 再生を修正**  
   reasoning 項目と対になる完了済みツール呼び出しを、stateless 再生で結果と一緒に取り除くようにしました。サービス側ストレージがない複数ターン／複数エージェントのワークフローで、孤立した呼び出しを送る問題を防ぎます。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7233?WT.mc_id=DT-MVP-5004827)

9. **承認ラウンドトリップ時の関数呼び出し重複を修正**  
   会話メッセージ全体で復元される関数呼び出しを重複排除し、Responses API の未対応ツール出力による 400 エラーを防止しました。公開 API の変更を伴わない回帰修正です。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7271?WT.mc_id=DT-MVP-5004827)

11. **GitHub Copilot の入力添付をインライン blob として転送**  
   base64 の `DataContent` を SDK の `BlobAttachment` に変換し、ストリーミングと非ストリーミングの両方で転送するようにしました。画像や文書が入力から黙って失われる問題を防ぎます。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7300?WT.mc_id=DT-MVP-5004827)

### Foundry、認証、運用

1. **FoundryToolbox の再接続で認証情報を保持**  
   OAuth 同意後にクライアントが閉じても、認証済み HTTP クライアントを遅延再生成できるようにしました。同じセッションを再開した後も認証情報を失わない、内部ライフサイクルの修正です。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7202?WT.mc_id=DT-MVP-5004827)

3. **FoundryToolbox で非同期認証情報をサポート**  
   `FoundryToolbox` が非同期の認証情報を扱えるようにしました。PR では既存利用者に対する破壊的変更はないとされています。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7208?WT.mc_id=DT-MVP-5004827)

5. **ワークフロー設定と認証情報の露出を削減**  
   6つのワークフローで認証モード以外の構成設定を環境シークレットから読むようにし、DevFlow の実行ジョブだけに必要な Copilot 権限を与えました。ログに出る設定情報と資格情報を最小化する変更です。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7270?WT.mc_id=DT-MVP-5004827)

7. **DevFlow の専用認証を復元**  
   組織設定で run-scoped の資格情報を使えない状況に対応し、DevFlow の PR レビューと issue 再現で専用の `GH_COPILOT_TOKEN` を再び使えるようにしました。ほかの GitHub App ベースのリポジトリ操作には影響しません。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7276?WT.mc_id=DT-MVP-5004827)

9. **高コストなワークフローログを修正**  
   進行しないまま再開したワークフローの警告ログを、ソース生成を使う型付きログメソッドへ移しました。ログ出力の保守性と一貫性を改善する内部変更です。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7268?WT.mc_id=DT-MVP-5004827)

### .NET の機能拡張

1. **Magentic の言語・プロンプトをカスタマイズ可能に**  
   実験的 API として `WithResponseLanguage`、`WithPromptOverrides`、公開済みの `MagenticDefaultPrompts` を追加しました。Magentic マネージャーが出す内部メッセージを、利用側が言語やテンプレート単位で調整できます。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7263?WT.mc_id=DT-MVP-5004827)

3. **Community Toolkit VectorData パッケージへ切り替え**  
   .NET 側の実装を新しい Community Toolkit VectorData パッケージへ移行しました。PR の説明に詳細がないため、ここでは移行の事実に限定して紹介します。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/5694?WT.mc_id=DT-MVP-5004827)

### 品質ゲート、依存関係、リリース

1. **ライフサイクルに応じたパッケージカバレッジを強制**  
   Python パッケージの Development Status からカバレッジ方針を導き、Beta／stable の非除外パッケージに85%の行カバレッジを要求するようにしました。新規・昇格済みパッケージを自動で品質ゲートへ含めます。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7261?WT.mc_id=DT-MVP-5004827)

3. **agent-framework-hosting-a2a を alpha 版へ更新**  
   A2A アダプターを含む `agent-framework-hosting-a2a` を `1.0.0a260721` から `1.0.0a260723` へ更新しました。無関係な Python パッケージのバージョンは変更していません。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7282?WT.mc_id=DT-MVP-5004827)

5. **brace-expansion を 1.1.16 へ更新**  
   DevUI フロントエンドの `brace-expansion` を 1.1.12 から 1.1.16 へ更新しました。Dependabot の PR であり、リリースノートには CVE-2026-13149 対応のバックポートが含まれます。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7232?WT.mc_id=DT-MVP-5004827)

7. **Python パッケージを 1.12.1 リリースへ更新**  
   root／core を 1.12.1、OpenAI を 1.11.0、Foundry を 1.10.3 などへ更新しました。影響する依存関係だけを対象にし、core の下限バージョンは据え置いています。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7273?WT.mc_id=DT-MVP-5004827)

9. **agent-framework-github-copilot を安定版 1.0.0 へ昇格**  
   `agent-framework-github-copilot` を `1.0.0rc4` から正式版 1.0.0 へ昇格しました。ドキュメントから `--pre` 指定を取り除きましたが、公開 API の変更はありません。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7302?WT.mc_id=DT-MVP-5004827)

11. **Python パッケージ管理の検証を高速化**  
   変更されたパッケージだけを検証し、ロックに依存しない lower／upper の import プローブを並列化しました。PR の測定では、単一パッケージの検証時間を約294秒から約40秒へ短縮しています。  
   [PR を見る](https://github.com/microsoft/agent-framework/pull/7274?WT.mc_id=DT-MVP-5004827)

---

## m5stack/StackChan

集計期間内にマージされた PR はありませんでした。

---

## まとめ

- **microsoft/fluentui-blazor**（0件）: 集計期間内のマージはありませんでした。
- **microsoft/fluentui**（3件）: positioning API の情報拡張と Dependabot 運用の整理が中心です。
- **microsoft/aspire**（3件）: .NET 11 テンプレートのオプトイン対応に加え、CLI とテストの信頼性を改善しています。
- **dotnet/runtime**（34件）: Wasm／ReadyToRun、ランタイム、ネットワーク、診断、リリースにまたがる幅広い変更がありました。#130980 では ReadyToRun 形式のバージョンを上げる**破壊的変更**が明示されています。
- **microsoft/agent-framework**（19件）: A2A、ツール実行、Foundry 連携、認証、品質ゲート、パッケージリリースの改善が集中しました。
- **m5stack/StackChan**（0件）: 集計期間内のマージはありませんでした。

## 次の行動

- ReadyToRun を利用するツールや配布物がある場合は、#130980 の形式バージョン変更が影響しないか確認します。
- Agent Framework の A2A ホスティングを試す場合は、#7258 のアダプター追加と #7282 の alpha パッケージ更新をあわせて確認します。
